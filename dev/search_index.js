var documenterSearchIndex = {"docs":
[{"category":"section","location":"references/#References","page":"References","text":"Cha, S. (1968). A Simple Method for Derivation of Rate Equations for Enzyme-catalyzed              Reactions under the Rapid Equilibrium Assumption or Combined Assumptions              of Equilibrium and Steady State. Journal of Biological Chemistry 243, 820–825.\n\n\n\nHaldane, J. B. (1930). Enzymes (Longmans, Green and Co., London).\n\n\n\nHansen, N. and Ostermeier, A. (2001). Completely Derandomized Self-Adaptation in Evolution Strategies. Evolutionary Computation 9, 159–195.\n\n\n\nHastie, T.; Tibshirani, R. and Friedman, J. (2009). The Elements of Statistical Learning. 2nd Edition (Springer, New York, NY).\n\n\n\nKing, E. L. and Altman, C. (1956). A Schematic Method of Deriving the Rate Laws for Enzyme-Catalyzed              Reactions. The Journal of Physical Chemistry 60, 1375–1378.\n\n\n\nMonod, J.; Wyman, J. and Changeux, J.-P. (1965). On the nature of allosteric transitions: A plausible model. Journal of Molecular Biology 12, 88–118.\n\n\n\nSegel, I. H. (1975). Enzyme Kinetics: Behavior and Analysis of Rapid Equilibrium and                Steady-State Enzyme Systems (Wiley, New York).\n\n\n\nWegscheider, R. (1902). Über simultane Gleichgewichte und die Beziehungen zwischen              Thermodynamik und Reaktionskinetik homogener Systeme. Zeitschrift für Physikalische Chemie 39, 257–303.\n\n\n\n","title":"References"},{"category":"section","location":"fitting/loss_and_optimizers/#Loss-and-optimizers","page":"Loss & optimizers","text":"This page explains the loss function that fit_rate_equation minimizes and how to choose an optimization algorithm through Optimization.jl.","title":"Loss & optimizers"},{"category":"section","location":"fitting/loss_and_optimizers/#The-loss-function","page":"Loss & optimizers","text":"loss!(x, fp) computes a log-ratio loss. Parameters in x are in log space — the actual rate constants are exp.(x). For each of the N data points it squares the log-ratio of predicted to measured rate, then sums over all points and divides by N:\n\nloss = (1/N) Σᵢ (log(|predicted rateᵢ|) − log(|measured rateᵢ|))²","title":"The loss function"},{"category":"section","location":"fitting/loss_and_optimizers/#Centered-vs-uncentered","page":"Loss & optimizers","text":"The loss formula switches on fp.scale_k_to_kcat (see Normalized vs absolute rate):\n\nRelative mode (scale_k_to_kcat is a Real): each group's log-ratios are mean-subtracted before squaring. This makes the loss invariant to multiplying all rates in a group by the same factor.\nAbsolute mode (scale_k_to_kcat === nothing): the log-ratios are squared without centering. The absolute magnitude is scored, so the data must be in consistent per-enzyme turnover units.","title":"Centered vs uncentered"},{"category":"section","location":"fitting/loss_and_optimizers/#Sign-mismatch-penalty","page":"Loss & optimizers","text":"When the predicted rate sign disagrees with the measured sign, or when the prediction is zero, the log-ratio is replaced by a sentinel value of 10.0 in the working buffer. After the main loop, a flat 100.0 penalty per mismatched point is added to the total. In centered mode this prevents an all-mismatch group from contributing zero loss (the uniform sentinel would cancel under mean-subtraction); the post-hoc penalty keeps it positive.\n\nnote: Sign penalty with all reactants present\nThis penalty is harmless for most enzyme kinetic data. An assay usually omits at least one substrate or one product, so the net rate keeps one sign across the whole dataset and stays clear of zero. When every substrate and product is present, though, the net rate can pass through zero, and the penalty can steer the fit toward parameters that reproduce the sign of near-zero rates and fit the rest of the data poorly. Watch for this when your measurements straddle the reaction's equilibrium.","title":"Sign-mismatch penalty"},{"category":"section","location":"fitting/loss_and_optimizers/#Optimizers","page":"Loss & optimizers","text":"EnzymeRates fits with any optimization algorithm available through Optimization.jl. fit_rate_equation wraps loss! into an Optimization.OptimizationFunction, builds an OptimizationProblem, and calls Optimization.solve(prob, optimizer; …) with the optimizer you pass. EnzymeRates depends only on Optimization.jl and ships no solver itself, so install the sub-package for the algorithm you want and pass its optimizer object. Two pure-Julia choices cover most needs:\n\n] add OptimizationCMAEvolutionStrategy   # CMA-ES — recommended\n] add OptimizationBBO                     # BBO differential evolution\n\nCMAEvolutionStrategyOpt() from OptimizationCMAEvolutionStrategy is the recommended optimizer and the one the identify_rate_equation search uses: CMA-ES (Covariance Matrix Adaptation Evolution Strategy) handles the correlated, non-convex landscapes that the Haldane/Wegscheider parameter reduction produces. BBO_adaptive_de_rand_1_bin_radiuslimited() from OptimizationBBO is a solid alternative for cross-checking a fit against an independent global optimizer. Any other Optimization.jl solver works as well.","title":"Optimizers"},{"category":"section","location":"fitting/loss_and_optimizers/#Passing-solver-options","page":"Loss & optimizers","text":"fit_rate_equation separates two kinds of solver options:\n\nCommon options are named keyword arguments: maxtime, maxiters, abstol, reltol, and callback. These are the Optimization.jl common solver options every solver understands. maxtime and maxiters are always forwarded to Optimization.solve; abstol, reltol, and callback are forwarded only when set, so each solver keeps its own default otherwise.\nSolver-specific options go in solver_kwargs, a NamedTuple forwarded verbatim to Optimization.solve. When a key appears in both a named common option and solver_kwargs, the solver_kwargs value wins.\n\nn_restarts is separate from both: it sets how many independent optimizations fit_rate_equation runs from different random starting points, keeping the best result. Finding the global minimum of a rate equation — a ratio of polynomials — is NP-hard, so any single run can settle in a local minimum; restarting from independent points raises the chance of reaching the global optimum. In our tests on complex rate equations with datasets of 500–1000 points, n_restarts = 10–20 returned the same loss on every run.","title":"Passing solver options"},{"category":"section","location":"deriving/re_vs_ss/#Rapid-equilibrium-vs-steady-state","page":"Rapid equilibrium vs steady state","text":"Every step in a mechanism is either rapid equilibrium (RE) or steady state (SS). That single flag drives parameter count: RE steps cost one parameter each, while SS steps cost two.","title":"Rapid equilibrium vs steady state"},{"category":"section","location":"deriving/re_vs_ss/#The-flag-in-the-DSL","page":"Rapid equilibrium vs steady state","text":"In @enzyme_mechanism, ⇌ marks a step as rapid equilibrium and <--> marks it as steady state. The distinction is stored on each Step as the is_equilibrium field.","title":"The flag in the DSL"},{"category":"section","location":"deriving/re_vs_ss/#Parameter-count-per-step","page":"Rapid equilibrium vs steady state","text":"An RE binding step contributes one parameter: a dissociation constant Kd, rendered as K_<metabolite>_<form> (for example, K_S_E).\nAn RE isomerization step contributes one parameter: an isomerization constant Kiso, rendered as Kiso_<from>_to_<to> (for example, Kiso_ES_to_EP).\nAn SS binding step contributes two rate constants: kon_<met>_<form> and koff_<met>_<form>.\nAn SS isomerization step contributes two directed rate constants: k_<from>_to_<to> and k_<to>_to_<from>.","title":"Parameter count per step"},{"category":"section","location":"deriving/re_vs_ss/#A-concrete-comparison","page":"Rapid equilibrium vs steady state","text":"A one-substrate reaction barely distinguishes the two assumptions — the rapid-equilibrium and steady-state rate laws differ only in how three or four constants are named. The difference becomes obvious for even the simplest mechanisms with more than one substrate or product. Take an ordered bi-uni reaction — A binds, then B, the complex isomerizes, and P leaves — first as a rapid-equilibrium mechanism with steady-state catalysis:\n\nusing EnzymeRates\nre = @enzyme_mechanism begin\n    substrates: A, B\n    products:   P\n    steps: begin\n        E + A ⇌ E(A)\n        E(A) + B ⇌ E(A, B)\n        E(A, B) <--> E(P)\n        E(P) ⇌ E + P\n    end\nend\nparameters(re)\n\nSix parameters, and a compact rate law:\n\nprint(rate_equation_string(re))\n\nNow the same skeleton with every step made steady state:\n\nss = @enzyme_mechanism begin\n    substrates: A, B\n    products:   P\n    steps: begin\n        E + A <--> E(A)\n        E(A) + B <--> E(A, B)\n        E(A, B) <--> E(P)\n        E(P) <--> E + P\n    end\nend\nparameters(ss)\n\nNine parameters — each binding step trades its single K for an independent kon/koff pair — and the rate law is far larger:\n\nprint(rate_equation_string(ss))\n\nThe contrast is structural, not cosmetic. The rapid-equilibrium denominator has one term per reachable enzyme form — 1, A, A·B, and P, four terms — because every binding step factors out as a pre-equilibrium segment. The steady-state denominator keeps those same four terms but adds new ones in B and B·P: nothing factors out, so the King–Altman treatment carries a term for every enzyme-form pattern, with products of on/off rates throughout. Those extra terms give the steady-state equation qualitatively different behaviour — not the same form with renamed constants — and after thermodynamic reduction it still keeps more independent parameters, because each binding step starts with two rate constants instead of one. Mechanisms with random-order binding diverge even more drastically: their steady-state rate equations can carry squared concentration terms, which users can confirm for themselves by building a random-order mechanism and printing its rate equation.","title":"A concrete comparison"},{"category":"section","location":"deriving/re_vs_ss/#The-RE-assumption","page":"Rapid equilibrium vs steady state","text":"Rapid equilibrium assumes the binding step relaxes to equilibrium on a time scale much faster than the catalytic step. When that assumption holds, the single Kd is sufficient. When it does not hold, the full on/off pair is needed.\n\nThe The Cha / King–Altman algorithm solves the full Cha steady state regardless of whether individual steps are RE or SS; RE steps simply factor out of the rate matrix as pre-equilibrium segments, giving the familiar K_met_form notation in the denominator.","title":"The RE assumption"},{"category":"section","location":"deriving/textbooks/#Derivation-tutorial","page":"Derivation tutorial","text":"This tutorial shows how to define the reversible Michaelis–Menten mechanism and derive its rate equation as a String. After reading it, you will recognize the package's parameter names as the textbook constants you already know.","title":"Derivation tutorial"},{"category":"section","location":"deriving/textbooks/#Defining-the-mechanism","page":"Derivation tutorial","text":"Use @enzyme_mechanism to declare the mechanism. The ⇌ arrow marks a step as rapid equilibrium (RE); <--> marks it as steady state (SS).\n\njulia> using EnzymeRates\n\njulia> m = @enzyme_mechanism begin\n           substrates: S\n           products:   P\n           steps: begin\n               E + S ⇌ E(S)\n               E(S) <--> E(P)\n               E(P) ⇌ E + P\n           end\n       end;\n\njulia> m isa EnzymeMechanism\ntrue\n\nThe two binding steps are rapid equilibrium; the central isomerization E(S) <--> E(P) is steady state. That single SS step is the rate-limiting catalytic step.","title":"Defining the mechanism"},{"category":"section","location":"deriving/textbooks/#Deriving-the-rate-equation","page":"Derivation tutorial","text":"rate_equation_string returns the symbolic rate equation as a multi-line String (it does not print). Call print to display it without escaped newlines:\n\njulia> print(rate_equation_string(m))\n(; K_P_E, K_S_E, k_ES_to_EP, Keq, E_total) = params\n(; S, P) = concs\n# Haldane constraints:\nk_EP_to_ES = (1 / Keq) * K_P_E * (1 / K_S_E) * k_ES_to_EP\nv = E_total * (k_ES_to_EP * S / K_S_E - k_EP_to_ES * P / K_P_E) / (1 + P / K_P_E + S / K_S_E)\n\nThe default mode is Reduced. In Reduced mode the string has four sections:\n\nA params destructuring line listing the independent fitted parameters plus Keq and E_total.\nA concs destructuring line listing the concentration symbols.\nAn optional # Haldane constraints: (or # Wegscheider constraints:) section where each dependent rate constant is expressed in terms of Keq and the independent parameters.\nThe final v = E_total * (num) / (den) line.","title":"Deriving the rate equation"},{"category":"section","location":"deriving/textbooks/#Meaning-of-parameter-names","page":"Derivation tutorial","text":"Package symbol Role Units\nK_S_E Dissociation constant of substrate S from the E–S complex. For a one-substrate, one-product Michaelis–Menten enzyme it is analogous to the Michaelis constant Km under the rapid-equilibrium approximation, but it is not the same as Km for more complex mechanisms. M\nK_P_E Dissociation constant of product P from the E–P complex. M\nk_ES_to_EP Rate of conversion of enzyme species ES to enzyme species EP. 1/s\nk_EP_to_ES Rate of the reverse conversion, EP to ES. 1/s\nkon_S_E Rate constant for binding of S to E. 1/(s·M)\nkoff_S_E Rate of dissociation of S from the E–S complex. 1/s\nKeq Equilibrium constant of the overall reaction; always user-supplied. unitless\nE_total Active-site (protomer) concentration. For a monomer this equals the total enzyme concentration; for an allosteric enzyme with N catalytic subunits it is N × the oligomer concentration, so allosteric kcat is reported per active site. M","title":"Meaning of parameter names"},{"category":"section","location":"deriving/textbooks/#Inspecting-parameters-and-metabolites","page":"Derivation tutorial","text":"The @example block below shows the fitted-parameter tuple and the concentration symbols returned by parameters and metabolites:\n\nusing EnzymeRates\nm = @enzyme_mechanism begin\n    substrates: S\n    products:   P\n    steps: begin\n        E + S ⇌ E(S)\n        E(S) <--> E(P)\n        E(P) ⇌ E + P\n    end\nend\n(parameters(m), metabolites(m))\n\nparameters(m) returns the symbols the fitter expects: the three independent constants plus Keq and E_total. metabolites(m) returns the concentration symbols the rate equation reads at runtime.","title":"Inspecting parameters and metabolites"},{"category":"section","location":"deriving/textbooks/#Full-mode","page":"Derivation tutorial","text":"Passing Full lists all raw rate constants, before any thermodynamic reduction:\n\nparameters(m, Full)\n\nFull mode includes both k_ES_to_EP and k_EP_to_ES as independent symbols, so there is no constraint section. See Rapid equilibrium vs steady state for the contrast between RE and SS parameters, and how adding SS steps changes this list.","title":"Full mode"},{"category":"section","location":"developer/#Developer-/-Architecture","page":"Developer / Architecture","text":"This page is for maintainers and contributors. It documents the internal architecture of EnzymeRates.jl; the public API lives on the API Reference page.\n\nThe package keeps two parallel mechanism representations, split along a deliberate performance boundary. Singleton types drive the compile-time rate-equation derivation; concrete types drive the mechanism enumeration. The two halves and the optimizer layer are described below.","title":"Developer / Architecture"},{"category":"section","location":"developer/#Derivation-architecture","page":"Developer / Architecture","text":"A mechanism's rate equation is derived once, at compile time, by the King–Altman/Cha method in @generated functions (src/rate_eq_derivation.jl). To make that possible, a mechanism is encoded as a singleton type — the exported EnzymeMechanism{Sig} or AllostericEnzymeMechanism{CatalyticMech, CatSites, RegSites} (src/types.jl) — that carries all of the mechanism's structure as Julia type parameters, canonicalized so that the order or direction in which the steps were written does not change the resulting type. The compiler specializes the derivation per type, so the symbolic King–Altman work runs during precompilation rather than at every call.\n\nThis is the most important architectural decision in the package, and it is deliberate. Moving the derivation to compile time leaves rate_equation(m, conc, params) as a flat numeric expression that must be allocation-free and sub-100 ns per call, enforced by test_rate_equation_performance (test/test_rate_eq_derivation.jl, asserting allocs == 0 and t < 100e-9 for every fixture mechanism). That speed is the binding constraint on the whole package: the fitter is a multi-start, global, gradient-free optimizer that evaluates rate_equation millions of times per fit, and a single rate equation can take minutes to fit, so any per-call allocation or microsecond-scale overhead would make fitting — and therefore identify_rate_equation, which fits thousands of candidates — impractical. loss! is held to the same standard: FittingProblem pre-allocates its log_ratios_buffer once and loss! reuses it, so the inner optimization loop allocates nothing. The cost is the flip side of the benefit: each unique singleton type triggers a full symbolic derivation, and a mechanism with many enzyme forms or steps can be slow to compile, exhaust memory, or StackOverflow.\n\ncompile_mechanism (internal) is the boundary between the two representations: compile_mechanism(m::Mechanism) = EnzymeMechanism(m) and compile_mechanism(am::AllostericMechanism) = AllostericEnzymeMechanism(am). The lift EnzymeMechanism(m::Mechanism) first drops regulators declared on the reaction but bound by no step, so they neither appear in regulators nor add a parameter; Mechanism(em) lifts back.","title":"Derivation architecture"},{"category":"section","location":"developer/#Enumeration-engine-architecture","page":"Developer / Architecture","text":"Mechanism enumeration uses the concrete types Mechanism and AllostericMechanism (src/types.jl) — internal, not exported — built directly from Step and Species values. Mechanism has two fields: reaction::EnzymeReaction and steps::Vector{Vector{Step}} — kinetic groups, one inner vector per group holding the steps that share that group's parameters. Step has from_species, to_species, bound_metabolite, and is_equilibrium. Like the singleton types, these are canonicalized so that the order or direction in which steps are written does not change the resulting mechanism.\n\nThese are ordinary value types to avoid excessive precompilation costs. The enumeration builds, expands, and deduplicates many thousands of candidate mechanisms (see The enumeration engine). If each candidate were a singleton parametric type, merely constructing it would trigger compiler specialization — the same precompilation cost the derivation pays — multiplied across thousands of mechanisms, which would be prohibitive. Carrying mechanism structure as runtime values instead means enumeration and deduplication cost no compilation at all. Only the candidates the search actually fits are lifted to singleton types, one at a time, through compile_mechanism.","title":"Enumeration engine architecture"},{"category":"section","location":"developer/#Optimization-algorithm-architecture","page":"Developer / Architecture","text":"Fitting depends only on Optimization.jl; the package ships no solver of its own. fit_rate_equation (src/fitting.jl) wraps loss! into an Optimization.OptimizationFunction, builds an OptimizationProblem, and calls Optimization.solve with whatever optimizer the caller passes. This gives the package access to the global, gradient-free optimizers that non-convex rate-equation fitting needs — CMA-ES (OptimizationCMAEvolutionStrategy) and BBO differential evolution (OptimizationBBO) are the tested choices — without taking on a solver dependency or locking users into one algorithm. Optimization.jl common options (maxtime, maxiters, …) are named keyword arguments, and solver-specific options pass through a solver_kwargs named tuple; see Loss & optimizers for the user-facing view.","title":"Optimization algorithm architecture"},{"category":"section","location":"fitting/normalized_vs_absolute/#Normalized-vs-absolute-rate","page":"Normalized vs absolute rate","text":"Enzyme kinetic data is often reported on an arbitrary rate scale. The enzyme concentration in the assay may be unknown, and figures from different preparations may reflect different states of purity, so the absolute value of a measured rate carries no reliable meaning. What survives is the shape of the response: changing the amount of enzyme scales every rate by the same factor and leaves that shape intact.\n\nEnzymeRates.jl handles both cases through one FittingProblem field, scale_k_to_kcat. For a trustworthy per-enzyme turnover scale, set it to nothing; the fit then scores absolute magnitudes. For an arbitrary scale, set it to a number — the enzyme's kcat if you know it, or simply 1; the fit then becomes invariant to that arbitrary factor and pins the returned turnover to the number you chose. That single field controls two things at once: which loss formula the fitter uses, and how it normalizes the returned parameters.","title":"Normalized vs absolute rate"},{"category":"section","location":"fitting/normalized_vs_absolute/#The-two-modes","page":"Normalized vs absolute rate","text":"","title":"The two modes"},{"category":"section","location":"fitting/normalized_vs_absolute/#Relative-mode-(the-default)","page":"Normalized vs absolute rate","text":"Pass a positive Real — the default is 1.0. In relative mode:\n\nLoss: each group's log-ratios are mean-centered before squaring, which removes the arbitrary per-group E_total scale. The loss is invariant to rescaling all rates in a group by the same factor.\nOutput normalization: the fitted SS rate constants are rescaled so that the forward catalytic rate constant (kcat) equals scale_k_to_kcat. With the default 1.0, the returned equation has kcat = 1; the absolute turnover is recovered by multiplying by a separately measured kcat value. A custom target (e.g. scale_k_to_kcat = 42.0) gives kcat = 42 directly.\n\nRelative mode is the right choice when your data reports relative rates (arbitrary units, percent activity, or values normalized per some E_total estimate that you do not trust).","title":"Relative mode (the default)"},{"category":"section","location":"fitting/normalized_vs_absolute/#Absolute-mode","page":"Normalized vs absolute rate","text":"Pass nothing. In absolute mode:\n\nLoss: log-ratios are squared without centering. The absolute magnitude of the rate is scored, so the data must report per-enzyme turnover in consistent units.\nOutput: the raw fitted parameters are returned without kcat normalization. The data fixes the absolute scale.\n\nAbsolute mode is the right choice when your data is already on a per-enzyme turnover basis.\n\nnote: The fitter has no `E_total` input\nEvery prediction is evaluated at E_total = 1, so the loss compares your measured Rate against the per-enzyme turnover — in effect it fits Rate / E_total. The data table carries no E_total column, and the fitter ignores one if present. Relative mode makes this moot: per-group centering cancels any constant factor, E_total included. Absolute mode does not, so the measured Rate must already be per-enzyme — divide rates recorded at a known enzyme amount by that amount before fitting.","title":"Absolute mode"},{"category":"section","location":"fitting/normalized_vs_absolute/#Rescaling-after-the-fact","page":"Normalized vs absolute rate","text":"rescale_parameter_values(mechanism, params; scale_k_to_kcat) is the public API for kcat normalization. It rescales only the rate constants — the lowercase-k parameters (kon_…, koff_…, and the steady-state interconversion k_…) — while leaving the binding constants K, Keq, E_total, the allosteric L, and regulatory K values unchanged.\n\nThe rate constants are the only parameters that carry time in their units, and the measured rate is the only thing that supplies a time scale, so it can pin down only what lives in them. The binding constants, Keq, and E_total are concentrations or concentration ratios, and L is a pure number — none carry a time dimension, so the rate constrains them only as far as the response shape already does. Multiplying every rate constant by a common factor scales kcat by that factor (kcat is homogeneous of degree one in them), and the K's stay put: a rapid-equilibrium step stores its binding constant directly, so the rescaling never touches it, while a steady-state binding constant is the ratio koff/kon, in which the common factor cancels. One uniform rescaling therefore sets kcat to any target while leaving every other parameter fixed.\n\nfit_rate_equation calls this function internally in relative mode. You can also call it directly on any parameter NamedTuple to renormalize after the fact.\n\nusing EnzymeRates\n\nuni_uni = @enzyme_mechanism begin\n    substrates: S\n    products:   P\n    steps: begin\n        E + S <--> E(S)\n        E(S) <--> E + P\n    end\nend\n\n# These params have kcat = 3.0 (kon_P_ES is the bottleneck)\nparams = (koff_P_ES = 6.0, kon_P_ES = 3.0, kon_S_E = 4.0, Keq = 2.0, E_total = 1.0)\n\nrescaled = rescale_parameter_values(uni_uni, params; scale_k_to_kcat = 1.0)\nrescaled\n\nNotice that only the rate constants changed (koff_P_ES, kon_P_ES, kon_S_E); Keq and E_total are unchanged. Calling rescale_parameter_values again on rescaled with scale_k_to_kcat = 1.0 returns the same values — the internal kcat computation on rescaled gives ≈ 1.","title":"Rescaling after the fact"},{"category":"section","location":"roadmap/#Roadmap","page":"Roadmap","text":"The current scope of EnzymeRates.jl covers the three pillars described in Getting Started: deriving rate equations, fitting them to data, and identifying the best mechanism by cross-validation. The directions below are under consideration for future development.","title":"Roadmap"},{"category":"section","location":"roadmap/#Support-more-mechanisms","page":"Roadmap","text":"EnzymeRates derives and identifies a broad space of mechanisms, but several families are not yet covered.\n\nKNF (sequential) allostery. The current MWC model treats all subunits as switching conformation in concert. A Koshland–Némethy–Filmer (sequential) model would let subunits change conformation individually, capturing a complementary class of cooperative behavior.\n\nIso mechanisms. In an iso mechanism the enzyme takes on a different conformation as it converts from the substrate-bound to the product-bound species, and that conformation relaxes back to the original only through a separate, slow steady-state isomerization step (Iso mechanisms works through an example). The derivation already handles these, but the enumeration does not yet generate iso mechanisms, so identify_rate_equation cannot reach them.","title":"Support more mechanisms"},{"category":"section","location":"roadmap/#Support-more-measurement-types","page":"Roadmap","text":"Fitting and identification currently work from reaction-rate (turnover) data. A planned extension would support binding and affinity observables as well: the fractional binding of one or more metabolites to the enzyme, an apparent Km, or a metabolite dissociation constant Kd. A mechanism could then be derived against and constrained by binding measurements, not rate alone, and report these quantities directly.","title":"Support more measurement types"},{"category":"section","location":"roadmap/#Parameter-estimation-and-identifiability","page":"Roadmap","text":"Beyond selecting the best mechanism, a planned analysis would characterize its parameters. Bootstrap resampling of the data would estimate a distribution — and hence a confidence interval — for each fitted parameter rather than a single point value. An accompanying identifiability analysis would flag parameters, individually or in combination, that the data cannot constrain, giving a clear picture of what the experiment actually resolves.","title":"Parameter estimation and identifiability"},{"category":"section","location":"roadmap/#Plotting","page":"Roadmap","text":"There is no built-in visualization. A planned plotting module would overlay fitted rate equations on data, making it straightforward to inspect fit quality and compare the best and runner-up mechanisms visually.","title":"Plotting"},{"category":"section","location":"roadmap/#Outlier-dataset-identification","page":"Roadmap","text":"The cross-validation framework already computes per-fold losses. A planned extension would use those fold losses to flag measurement groups that the selected mechanism fits poorly — surfacing suspect datasets or experimental conditions that violate the assumed mechanism.","title":"Outlier dataset identification"},{"category":"section","location":"identify/enumeration_engine/#The-enumeration-engine","page":"The enumeration engine","text":"The enumeration engine is the core of EnzymeRates.jl: it defines the universe of enzyme mechanisms that identify_rate_equation can sample. The engine starts from the simplest mechanisms and applies a fixed set of moves that each turn one mechanism into a slightly more complex one, so the search visits candidates in order of increasing complexity. A rate equation the engine cannot construct is never fit, and so can never be selected.\n\nAt a high level the engine has two halves. EnzymeRates.init_mechanisms builds the starting set: the minimum-parameter catalytic mechanisms for a reaction — each binding order, with optional dead-end substrate and product inhibition — at their lowest parameter count. EnzymeRates.expand_mechanisms then grows the set through a fixed set of single moves, each taking a mechanism and returning slightly more complex children: splitting a shared rate constant, flipping a rapid-equilibrium step to steady state, adding a regulator, making the enzyme allosteric, and so on. After every step the candidates are deduplicated, since different move sequences can sometimes reach the same mechanism. Both functions are internal, but understanding them explains exactly which equations the search can reach.","title":"The enumeration engine"},{"category":"section","location":"identify/enumeration_engine/#EnzymeRates.init_mechanisms","page":"The enumeration engine","text":"EnzymeRates.init_mechanisms(reaction) returns the minimum-parameter mechanisms for a reaction. For each catalytic topology it enumerates all substrate/product dead-end subsets, assigns one steady-state catalytic step, and collapses binding steps that share the same (metabolite, RE/SS) class into a single kinetic group — the lowest-parameter starting point for the rate-equation search. When a reaction's atom inventory allows a covalent fragment to persist between half-reactions, it also builds ping-pong mechanisms: the modified enzyme stays on conformation :E carrying a residual rather than a separate conformation label, and a step that would return the enzyme to free :E with an empty residual mid-cycle is rejected, since it would split the reaction into two disconnected half-cycles. See Ping-pong mechanisms for detail on these mechanisms.","title":"EnzymeRates.init_mechanisms"},{"category":"section","location":"identify/enumeration_engine/#EnzymeRates.seed_mechanisms","page":"The enumeration engine","text":"For a reaction that declares regulators, init_mechanisms starts far below the useful region: every seed binds zero regulators, so the search must climb through every partially-regulated mechanism before it reaches one that binds them all. When the effectors are already known — phosphofructokinase and its five regulators, say — that lower shelf is pure waste.\n\nEnzymeRates.seed_mechanisms(reaction, required_allosteric, required_competitive) starts the search where it belongs. It grows init_mechanisms through the regulator-binding moves alone — go allosteric, add an allosteric regulator, add a competitive inhibitor — under three constraints that keep the set small: each required regulator binds at its own single-ligand site, every allosteric state stays cheap (:OnlyA/:OnlyI, never :NonequalAI), and no partially-regulated mechanism survives. The result is every fully-regulated mechanism at its minimum parameter count, and nothing beneath it. The beam then refines these seeds with the detail moves — steady-state flips, splits, and :NonequalAI relaxations — as usual.\n\nBy default every declared regulator is required. identify_rate_equation's optional_allosteric_regulators and optional_competitive_inhibitors keywords move named regulators back to optional, so the beam adds them as refinements rather than forcing them into every seed; listing every regulator as optional recovers the init_mechanisms starting set. Declaring a regulator's type in the reaction — ::Activator or ::Inhibitor — pins it to one allosteric state and shrinks the set further. The Identify tutorial works a concrete example.","title":"EnzymeRates.seed_mechanisms"},{"category":"section","location":"identify/enumeration_engine/#The-seven-expansion-moves","page":"The enumeration engine","text":"EnzymeRates.expand_mechanisms(mechs, rxn) applies all seven moves to every input mechanism and returns the resulting child mechanisms pooled into one list. Each move is applied in every applicable way, so one input mechanism yields many children — every rapid-equilibrium group that can flip to steady state, every step that can be split into its own group, every way an inhibitor can bind, and so on. Every child is checked to conserve atoms before it is returned.\n\nThe seven moves:","title":"The seven expansion moves"},{"category":"section","location":"identify/enumeration_engine/#1.-Flip-a-rapid-equilibrium-group-to-steady-state","page":"The enumeration engine","text":"Flips one entire kinetic group from rapid equilibrium (RE) to steady state (SS), atomically — all steps in the group convert together. The move is a no-op for a group already in SS.\n\nParameter delta: +1 for most groups (the SS reverse rate is a new independent parameter). A Haldane/Wegscheider constraint can make the reverse rate dependent on existing parameters, giving a net +0 — which is why the search counts each mechanism's actual fitted parameters rather than assuming a fixed +1 per move.","title":"1. Flip a rapid-equilibrium group to steady state"},{"category":"section","location":"identify/enumeration_engine/#2.-Give-one-step-its-own-kinetic-group","page":"The enumeration engine","text":"For each kinetic group with two or more steps, carves one step into a fresh singleton group. The split step then has an independent rate constant rather than sharing one with its former group members.\n\nParameter delta: +1 in the simplest case — the split-off constant becomes independent. As with the RE→SS move, a Haldane/Wegscheider constraint can make that constant dependent instead, giving a net +0; the search counts each mechanism's actual fitted parameters rather than assuming +1.","title":"2. Give one step its own kinetic group"},{"category":"section","location":"identify/enumeration_engine/#3.-Add-a-competitive-inhibitor-binding-site","page":"The enumeration engine","text":"Adds binding steps for a CompetitiveInhibitor declared in the reaction, as a dead-end complex with the enzyme. The move enumerates the combinations of enzyme species the inhibitor can bind, emitting one child mechanism per combination, subject to two rules:\n\nBinding capacity. An enzyme form holds at most as many metabolites as the larger of the substrate and product counts, max(#substrates, #products), so the inhibitor is not added to a form already at capacity.\nMirror steps. If the inhibitor binds two enzyme forms that a catalytic step already connects, a mirror step is added between the two inhibitor-bound forms, so the inhibitor-bound branch stays connected to the cycle. Each mirror inherits its catalytic counterpart's kinetic group and adds no parameter.\n\nThe inhibitor's own binding steps form one fresh kinetic group (one new dissociation constant K_R).\n\nParameter delta: +1 per competitive-inhibitor group added.","title":"3. Add a competitive inhibitor binding site"},{"category":"section","location":"identify/enumeration_engine/#4.-Promote-a-non-allosteric-to-allosteric-mechanism","page":"The enumeration engine","text":"Converts a Mechanism to an AllostericMechanism variant set. For each kinetic group, one variant sets that group to :OnlyA (the rest stay :EqualAI); the all-:EqualAI baseline is never emitted, since the two conformations would then be identical and L would cancel. The engine emits only thermodynamically feasible variants: :OnlyA asserts K_A/K_I → ∞ on a binding or kcat_A/kcat_I → ∞ on catalysis, and a one-sided :OnlyA binding would leave the inactive conformation unable to close its catalytic cycle (see Thermodynamic constraints of MWC equations). So a binding group set to :OnlyA is emitted only with the further promotion the constraint forces — the chemical step :OnlyA, or an opposing binding :OnlyA — one variant per minimal feasible completion; the bound metabolite's concentration reveals L (a K-type mechanism). A catalytic (isomerization) group set to :OnlyA is emitted paired with a declared allosteric regulator, one variant per (regulator, tag) with tag ∈ {:OnlyA, :OnlyI} — the regulator makes L identifiable (a V-type mechanism). Enumeration runs over allowed_catalytic_multiplicities. No-op on an already allosteric input.\n\nIn MWC terminology the A-state corresponds to the R-state and the I-state to the T-state of the original Monod–Wyman–Changeux nomenclature; this package uses A/I throughout.\n\nParameter delta: +1 for a K-type variant — the conformational equilibrium constant L is the sole new parameter. +2 for a V-type variant — L plus the paired regulator's binding constant. :OnlyA zeroes out the I-state and adds no parameter of its own.","title":"4. Promote a non-allosteric to allosteric mechanism"},{"category":"section","location":"identify/enumeration_engine/#5.-Add-an-allosteric-ligand","page":"The enumeration engine","text":"Adds one AllostericRegulator at a new or an existing regulatory site, with an allosteric-state tag drawn from {:OnlyA, :OnlyI, :NonequalAI} (plus :EqualAI only at a mixed existing site). No-op on a non-allosteric input.\n\nParameter delta:\n\n:OnlyA or :OnlyI tag: +1 (one binding constant K_A or K_I).\n:NonequalAI tag: +2 (independent K_A and K_I).","title":"5. Add an allosteric ligand"},{"category":"section","location":"identify/enumeration_engine/#6.-Relax-an-:EqualAI-group-to-independent-A/I-states","page":"The enumeration engine","text":"Changes one :EqualAI or :OnlyA/:OnlyI allosteric-state tag to :NonequalAI, giving the active-state and inactive-state versions of the group independent parameters. No-op on a non-allosteric input.\n\nParameter delta:\n\nRE binding group: +1 (one shared K splits into K_A and K_I).\nSS rate group: +2 (each SS rate kf and kr splits into A/I pairs, adding two new independent constants).","title":"6. Relax an :EqualAI group to independent A/I states"},{"category":"section","location":"identify/enumeration_engine/#7.-Merge-two-regulatory-sites","page":"The enumeration engine","text":"Combines two regulatory sites into one shared site, so their ligands compete for it rather than binding independently. For each merged pair the move also enumerates the antagonist forms: one ligand may switch to :EqualAI — binding both conformations equally, with no allosteric effect of its own — so that it acts purely by displacing the other ligand from the shared site. An activator that displaces an inhibitor still reads as activation, and an inhibitor that displaces an activator still reads as inhibition, so the observable effect is preserved. No-op on a non-allosteric input or a single-site mechanism.\n\nParameter delta: +0. Each ligand keeps its one binding constant; only the binding topology changes. The merged mechanism is a distinct rate equation, which the beam fits alongside the independent-site form so that cross-validation can choose between them.","title":"7. Merge two regulatory sites"},{"category":"section","location":"identify/tutorial/#Identify-tutorial","page":"Identify tutorial","text":"identify_rate_equation takes an EnzymeReaction and rate data, enumerates biochemically valid mechanisms, and fits the most promising candidates at each parameter count — fitting every mechanism is far too expensive — returning the simplest one that generalizes by leave-one-group-out cross-validation.\n\nThis page walks through a fully runnable example: an MWC allosteric enzyme, recovered from noiseless data in about a minute. The full production search widens the beam to the defaults (min_beam_width=50, loss_rel_threshold=2.0, loss_abs_threshold=0.01, loss_parsimony_threshold=1.01, max_param_count=20, eq_complexity_filter=337) and would often run for many hours and require a High Performance Compute cluster (see Running in parallel).","title":"Identify tutorial"},{"category":"section","location":"identify/tutorial/#A-reaction-and-a-generating-mechanism","page":"Identify tutorial","text":"The example is a reversible uni-uni reaction S ⇌ P run by a dimeric enzyme with an allosteric activator A. The reaction declares the regulator and the oligomeric state; from these The enumeration engine builds the MWC variants the search fits.\n\nusing EnzymeRates\n\nrxn = @enzyme_reaction begin\n    substrates: S[C]\n    products:   P[C]\n    allosteric_regulators: A\n    oligomeric_state: 2\nend\n\n# A concrete MWC mechanism to generate the data. A binds only the active\n# conformation (:OnlyA), which makes it an allosteric activator.\ngenerator = @allosteric_mechanism begin\n    substrates: S\n    products:   P\n    catalytic_multiplicity: 2\n    allosteric_regulators: A::OnlyA\n    catalytic_steps: begin\n        E + S ⇌ E(S)         :: EqualAI\n        E(S) <--> E(P)       :: OnlyA\n        E(P) ⇌ E + P         :: EqualAI\n    end\nend\n\nprintln(\"fitted params: \", EnzymeRates.fitted_params(generator))\nprintln(\"metabolites:   \", metabolites(generator))\n\nThe mechanism has five independent parameters: the binding constants K_S_E and K_P_E (shared by both conformations, :EqualAI), the active-state catalytic constant k_A_ES_to_EP (:OnlyA), the activator binding constant K_A_Areg, and the conformational equilibrium L = [T]/[R]. Keq is user-supplied and E_total is absorbed into the rate scale. Mechanisms with allosteric regulators covers the allosteric-state tags and the partition-function structure.\n\nEach substrate and product declares its atom inventory in the bracket: S[C] is one carbon, and A[C2N1] would be two carbons and one nitrogen. These counts let the enumerator enforce atom conservation across steps and recognise covalent (ping-pong) intermediates; this transfer needs only a single [C] placeholder.","title":"A reaction and a generating mechanism"},{"category":"section","location":"identify/tutorial/#Simulate-noiseless-data","page":"Identify tutorial","text":"We evaluate the generator on a concentration grid. The activator A spans well below to well above its binding constant, so the grid fully samples the allosteric response, and Keq = 10 keeps every net rate strictly positive — a rate of exactly zero has no logarithm, and the loss works in log space.\n\nKeq = 10.0\ntrue_params = (K_S_E = 1.0, K_P_E = 1.0, k_A_ES_to_EP = 5.0,\n               K_A_Areg = 1.0, L = 100.0, Keq = Keq, E_total = 1.0)\n\nconcs = [(S = s, P = p, A = a)\n         for s in (0.3, 1.0, 3.0, 10.0) for p in (0.1, 0.5)\n         for a in (0.03, 0.3, 3.0, 30.0)]\ngroups = [i ≤ length(concs) ÷ 2 ? \"G1\" : \"G2\" for i in eachindex(concs)]\n\ndata = (group = groups,\n        Rate  = [rate_equation(generator, c, true_params) for c in concs],\n        S = [c.S for c in concs],\n        P = [c.P for c in concs],\n        A = [c.A for c in concs])\nnothing # hide\n\nThe data table has a :group column, a :Rate column, and one column per metabolite — substrate, product, and the regulator A. Each unique group value becomes one cross-validation fold, so at least two groups are required.","title":"Simulate noiseless data"},{"category":"section","location":"identify/tutorial/#The-data-contract","page":"Identify tutorial","text":"IdentifyRateEquationProblem validates the table at construction:\n\n:group and :Rate columns must be present.\nOne column per substrate, product, and regulator (names match metabolites(mechanism) exactly).\nEvery Rate must be nonzero — the loss function works in log space.\nAt least two distinct group values are required for cross-validation.\n\nKeq is a required keyword argument, always user-supplied; the package never estimates it from data. Most enzyme reactions have a known Keq — measure it directly, or compute it from a resource such as eQuilibrator.","title":"The data contract"},{"category":"section","location":"identify/tutorial/#Run-the-search","page":"Identify tutorial","text":"using OptimizationCMAEvolutionStrategy, Random\nRandom.seed!(123)   # reproducible multi-start fits\n\nprob = IdentifyRateEquationProblem(rxn, data; Keq = Keq)\n\nresults = identify_rate_equation(prob;\n    optimizer       = CMAEvolutionStrategyOpt(),\n    min_beam_width  = 10,\n    max_param_count = 5,\n    n_restarts      = 5,\n    maxtime         = 4.0,\n)\nnothing # hide\n\nThe search does not start from scratch. Its seed mechanisms are the simplest catalytic mechanisms for the reaction — one per binding order, with optional dead-end substrate and product inhibition, each at its lowest parameter count. Because this reaction declares A, the seeds are lifted a level: the search starts from mechanisms that already bind A — every fully-regulated mechanism at its minimum parameter count — and never fits the non-allosteric mechanisms beneath. This is the default: every declared regulator is required, which is what lets the search reach the generating MWC mechanism so quickly here.\n\nThe progress lines above trace the search, which is a beam search: it walks parameter counts in ascending order, and at each count it fits the candidates, keeps the most promising (the beam — here min_beam_width = 10), and expands the survivors into the next count. Which mechanisms stay in the beam is set by min_beam_width and the loss_rel_threshold / loss_abs_threshold / loss_parsimony_threshold cutoff — see Best mechanism selection. Two filters bound the search: max_param_count (here 5, the generating mechanism's size, to keep the example quick) caps it by fitted-parameter count, and eq_complexity_filter caps it by rate-equation complexity — roughly the number of terms in the equation's denominator, its default passing a fully steady-state random-order bi-bi. Each drops a candidate before fitting it. Best mechanism selection also covers the cross-validation rule that picks the winner. The production search widens the beam to 50 and the cap to 20, and distributes the fits across workers (see Running in parallel).\n\nTwo controls tune the regulator seeding. To let the search decide whether A matters, and fit the non-regulated mechanisms too, mark it optional:\n\nidentify_rate_equation(prob; optimizer = CMAEvolutionStrategyOpt(),\n                       optional_allosteric_regulators = [:A])\n\nTo go the other way and shrink the seed set, declare A's type. An activator binds the active conformation, so A::Activator pins it to :OnlyA and halves the seeds an undesignated regulator would otherwise produce:\n\nallosteric_regulators: A::Activator\n\nThe enumeration engine explains how the seed set is built, and the Roadmap tracks the moves that refine it.","title":"Run the search"},{"category":"section","location":"identify/tutorial/#Read-the-result","page":"Identify tutorial","text":"IdentifyRateEquationResults has exactly two fields: best and cv_results.\n\nresults.best\n\nresults.best is an AbstractEnzymeMechanism. Pass it to rate_equation_string to see the symbolic rate equation:\n\nprint(rate_equation_string(results.best))\n\nOn noiseless data the search recovers the generating MWC mechanism. The active and inactive conformations enter as a partition function, (active polynomial)² + L·(inactive polynomial)² — squared because the enzyme is a dimer — the activator contributes (1 + A/K_A_Areg)² to the active state, and the Haldane constraint fixes the dependent reverse rate k_A_EP_to_ES from Keq and the independent constants.\n\nresults.cv_results is a DataFrame with one row per candidate equation that entered cross-validation, scored as detailed on the Best mechanism selection page:\n\nfirst(results.cv_results, 5)\n\nresults.cv_results and the selected results.best are also written to save_dir, as loocv_results.csv (this whole table) and best_equation.csv (the winning row: equation string plus fitted parameters). They sit alongside the per-iteration equation_search_iteration_N.csv files, so a cluster run's model-selection outcome is saved without re-running cross-validation.","title":"Read the result"},{"category":"section","location":"identify/tutorial/#Loud-failures","page":"Identify tutorial","text":"A mechanism that throws during compilation or fitting becomes a FitFailure carrying the exception text. Failures are never silently discarded — they appear in cv_results (and the saved CSVs) with the retcode and error columns populated. If every mechanism in the base tier fails, the search re-raises the first exception, so an unsupported optimizer keyword or a memory overflow surfaces immediately rather than being swallowed.","title":"Loud failures"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"This page lists every exported name. Maintainer-only internals are not shown here — see the Developer / Architecture page for those.","title":"API Reference"},{"category":"section","location":"api/#Index","page":"API Reference","text":"Order = [:function, :macro, :type, :constant]","title":"Index"},{"category":"section","location":"api/#Functions","page":"API Reference","text":"","title":"Functions"},{"category":"section","location":"api/#Macros","page":"API Reference","text":"","title":"Macros"},{"category":"section","location":"api/#Types","page":"API Reference","text":"","title":"Types"},{"category":"section","location":"api/#Constants","page":"API Reference","text":"","title":"Constants"},{"category":"method","location":"api/#EnzymeRates.fit_rate_equation-Tuple{FittingProblem, Any}","page":"API Reference","text":"fit_rate_equation(fp::FittingProblem, optimizer;\n    n_restarts=20, maxtime=60.0, maxiters=10_000_000,\n    abstol=nothing, reltol=nothing, callback=nothing,\n    lb=fill(-15.0, length(fitted_params(fp.mechanism))),\n    ub=fill(15.0, length(fitted_params(fp.mechanism))),\n    solver_kwargs=(;))\n\nFit rate constants by minimizing loss! using Optimization.jl.\n\nRuns n_restarts independent optimizations from random initial points and returns the best result.\n\nmaxtime and maxiters are Optimization.jl common solver options forwarded to every Optimization.solve call. abstol, reltol, and callback are common options forwarded only when set (otherwise each solver uses its own default). solver_kwargs is a NamedTuple of solver-specific options forwarded verbatim to solve (e.g. (; popsize=200) for a CMA-ES solver that supports it). A key present in both a named common option and solver_kwargs takes the solver_kwargs value. lb/ub are the log-space bounds passed to the OptimizationProblem.\n\nRescaling is driven by fp.scale_k_to_kcat: when it is a Real, the returned parameters are rescaled so that _kcat_forward(mechanism, params) ≈ fp.scale_k_to_kcat; when it is nothing, the raw (unrescaled) parameters are returned (the data fixes the absolute scale).\n\nReturns a NamedTuple (params, loss, retcode) where:\n\nparams: fitted rate constants as a NamedTuple\nloss: the best loss value achieved\nretcode: the Symbol form of the best restart's sol.retcode. Only :Success indicates the optimizer converged on its own criteria; any other value (e.g. :MaxTime — hit the time budget; :Failure; :Default — ran but did not flag success) means the fit should be treated as un-converged (check retcode !== :Success).\n\n\n\n\n\n","title":"EnzymeRates.fit_rate_equation"},{"category":"method","location":"api/#EnzymeRates.identify_rate_equation-Tuple{IdentifyRateEquationProblem}","page":"API Reference","text":"identify_rate_equation(prob; optimizer,\n    min_beam_width=50, loss_rel_threshold=2.0, loss_abs_threshold=0.01,\n    loss_parsimony_threshold=1.01,\n    max_param_count=20, n_restarts=20, maxtime=60.0, maxiters=10_000_000,\n    abstol=nothing, reltol=nothing, callback=nothing, solver_kwargs=(;),\n    n_cv_candidates=5, se_threshold=1.0, perm_p_threshold=0.16,\n    save_dir=_default_save_dir(), show_progress=true)\n\nFind the best rate equation for the given reaction and data using beam search.\n\nKeyword Arguments\n\nmin_beam_width::Int = 50: minimum mechanisms to keep per param-count tier\nloss_rel_threshold::Float64 = 2.0: relative tolerance for beam selection (see \"Beam selection\" below)\nloss_abs_threshold::Float64 = 0.01: absolute tolerance for beam selection\nloss_parsimony_threshold::Float64 = 1.01: a mechanism keeps expanding only if its loss is within this factor of the best model of any smaller parameter count — an added parameter must earn its keep. Combined with the other loss thresholds via min; min_beam_width is a cumulative per-count budget (see \"Beam selection\" below). Inf disables it.\nmax_param_count::Int = 20: stop expanding beyond\neq_complexity_filter::Int = 337: skip (before fitting, before any derivation) any mechanism whose rate equation is more complex than this — roughly the number of terms in the rate equation's denominator: V×τ, the number of RE-segments times the spanning-tree count of the catalytic segment graph, i.e. the products the compiled equation evaluates on every call. The default 337 is one above a fully steady-state random-order bi-bi (V×τ = 336), so such a mechanism passes and anything more complex is skipped — equations denser than that are impractical to fit and can blow up derivation/codegen. Computed from the mechanism graph alone.\noptional_allosteric_regulators::Vector{Symbol} = Symbol[]: declared allosteric regulators the seed may omit. A reaction that declares regulators seeds the beam fully regulated by default; naming a regulator here lets the beam also start from seeds that do not bind it (added back later by expansion). Listing every declared allosteric regulator restores the unregulated init_mechanisms seed.\noptional_competitive_inhibitors::Vector{Symbol} = Symbol[]: declared competitive inhibitors the seed may omit, as above.\noptimizer: Optimization.jl optimizer (required). Recommended: CMAEvolutionStrategyOpt() from OptimizationCMAEvolutionStrategy.\nn_restarts::Int = 20: multi-start restarts per fit\nmaxtime::Real = 600.0: max time per fit (seconds; common solver option, forwarded to Optimization.solve)\nmaxiters::Integer = 10_000_000: max iterations per optimizer run (common solver option, forwarded to Optimization.solve)\nabstol/reltol/callback = nothing: Optimization.jl common solver options, forwarded to Optimization.solve only when set\nsolver_kwargs::NamedTuple = (;): solver-specific options forwarded verbatim to Optimization.solve (e.g. (; popsize=200) for a CMA-ES solver that supports it); the caller matches its contents to optimizer\nn_cv_candidates::Int = 5: LOOCV top N unique-rate-equation candidates per param count\nse_threshold::Float64 = 1.0: paired 1-SE multiplier for model selection. Simpler-model bucket accepted iff its mean paired log-loss difference vs the best bucket is ≤ se_threshold * std(diffs)/sqrt(n_folds). Default 1.0 is the textbook \"1-SE rule\".\nperm_p_threshold::Float64 = 0.16: minimum one-sided permutation p-value for model selection. Simpler-model bucket accepted iff p > perm_p_threshold under the sign-flip null. Default 0.16 matches paired 1-SE empirically.\nsave_dir::String = _default_save_dir(): output directory for the search CSVs (initial_mechanisms.csv + equation_search_iteration_N.csv), plus loocv_results.csv (the LOOCV table for every cross-validated candidate) and best_equation.csv (the selected equation and its fitted parameters)\n\nBeam selection\n\nA mechanism at parameter count n qualifies for the next-level beam if either:\n\nits loss ≤ min(loss_rel_threshold * best(n) + loss_abs_threshold, loss_parsimony_threshold * best(<n)), where best(n) is the lowest loss at parameter count n and best(<n) is the lowest loss over all smaller counts; the second term is dropped at the base count (no smaller level exists yet),\nOR it falls under the min_beam_width budget: a cumulative per-count allowance that expands at least min_beam_width mechanisms at each count over the whole search, then stops — spent once, not re-granted each time the count is revisited.\n\nThe additive term protects against best_loss approaching zero (simulated / very-low-loss data) where a purely multiplicative threshold would collapse the beam to the single best mechanism.\n\nModel selection (LOOCV)\n\nFor each n_params bucket below n_min (lowest mean log-fold-loss) the rule computes paired log-loss differences between the bucket's representative and n_min's, then accepts the simpler bucket iff BOTH:\n\nPaired 1-SE rule: mean(diffs) ≤ se_threshold * std(diffs)/sqrt(n_folds).\nOne-sided permutation test: permutation_p > perm_p_threshold, where p is computed by exact enumeration when n_folds ≤ 20 and Monte Carlo (10⁶ samples) otherwise.\n\nReturns the smallest passing n_params; falls through to n_min if none pass. Within the chosen bucket the mechanism with lowest training loss wins. Per-bucket representative = the row with the lowest cv_score in that bucket. Diagnostic columns mean_log_loss_diff, se_paired, permutation_p are surfaced in cv_results; the n_min bucket has 0.0 in all three.\n\n\n\n\n\n","title":"EnzymeRates.identify_rate_equation"},{"category":"method","location":"api/#EnzymeRates.metabolites-Union{Tuple{EnzymeMechanism{Sig}}, Tuple{Sig}} where Sig","page":"API Reference","text":"metabolites(m::EnzymeMechanism) → Tuple{Symbol,...}\n\nReturn distinct metabolite names (substrates ∪ products ∪ regulators) as a tuple of Symbols in declaration order, deduplicated. The fitter uses this as the key set for the per-datapoint concentration NamedTuple passed to rate_equation.\n\njulia> using EnzymeRates\n\njulia> m = @enzyme_mechanism begin\n           substrates: S\n           products: P\n           steps: begin\n               E + S <--> E(S)\n               E(S) <--> E + P\n           end\n       end;\n\njulia> metabolites(m)\n(:S, :P)\n\n\n\n\n\n","title":"EnzymeRates.metabolites"},{"category":"function","location":"api/#EnzymeRates.parameters","page":"API Reference","text":"parameters(m::EnzymeMechanism, [mode])\nparameters(m::AllostericEnzymeMechanism, [mode])\n\nReturn the parameter names required for the given mode as a tuple of Symbols.\n\nModes\n\nReduced (default): independent k's + Keq + E_total. The set of symbols the user supplies to evaluate the Haldane-reduced rate equation. Returned for both EnzymeMechanism and AllostericEnzymeMechanism.\nFull: all raw rate-constant symbols + E_total. For EnzymeMechanism this is \"all 2N k's + E_total.\" For AllostericEnzymeMechanism it composes the catalytic raw A-state symbols + every I-state mirror (catalytic + regulatory + synthesized dep) + reg-site A-state K's (skipping :OnlyI ligands) + :L + :E_total. The allosteric Full mode enumerates the complete raw-symbol set; no rate_equation method is defined for (::AllostericEnzymeMechanism, ::FullMode), so this mode is for symbol enumeration, not runtime evaluation.\n\n\n\n\n\n","title":"EnzymeRates.parameters"},{"category":"function","location":"api/#EnzymeRates.rate_equation","page":"API Reference","text":"rate_equation(m, concs, params, [mode])\n\nReturn the net reaction rate of mechanism m at the metabolite concentrations concs and parameters params (both NamedTuples). The rate equation itself is derived from the mechanism's elementary steps by the King–Altman/Cha method: rapid-equilibrium steps collapse to binding constants, steady-state steps are assembled into the King–Altman determinant, and the two combine into the quasi-steady-state flux through the whole mechanism — so one call returns the overall turnover, not the rate of a single step.\n\nmode selects how parameters enter the equation. The default Reduced applies the Haldane/Wegscheider reduction, deriving the dependent rate constants from Keq and the independent parameters; Full instead takes every rate constant as independent.\n\nThe derivation runs once, at compile time: the body is generated as a single arithmetic expression with no allocations, loops, or matrix operations. Use rate_equation_string to inspect the derived equation symbolically.\n\n\n\n\n\n","title":"EnzymeRates.rate_equation"},{"category":"function","location":"api/#EnzymeRates.rate_equation_string","page":"API Reference","text":"rate_equation_string(m, [mode]) -> String\n\nReturn the symbolic rate equation for mechanism m as a multi-line String (it returns the text — it does not print). mode is Reduced (default) or Full; pass a concrete Mechanism / AllostericMechanism or its compiled EnzymeMechanism singleton.\n\nThe string is a runnable transcript of how rate_equation evaluates: a (; …) = params destructure line, a (; …) = concs destructure line, then the v = E_total * (num) / (den) line. In Reduced mode, dependent rate constants are listed first under # Wegscheider constraints: and # Haldane constraints: headers — the thermodynamic identities that eliminate parameters — and only the independent set appears in the params destructure. In Full mode every rate constant is independent, so there is no constraint section. Full mode is defined for EnzymeMechanism only; an AllostericEnzymeMechanism supports Reduced mode only.\n\nUse print on the result to see the multi-line layout without escaped newlines.\n\njulia> using EnzymeRates\n\njulia> m = @enzyme_mechanism begin\n           substrates: S\n           products: P\n           steps: begin\n               E + S ⇌ E(S)\n               E(S) <--> E(P)\n               E(P) ⇌ E + P\n           end\n       end;\n\njulia> print(rate_equation_string(m))\n(; K_P_E, K_S_E, k_ES_to_EP, Keq, E_total) = params\n(; S, P) = concs\n# Haldane constraints:\nk_EP_to_ES = (1 / Keq) * K_P_E * (1 / K_S_E) * k_ES_to_EP\nv = E_total * (k_ES_to_EP * S / K_S_E - k_EP_to_ES * P / K_P_E) / (1 + P / K_P_E + S / K_S_E)\n\n\n\n\n\n","title":"EnzymeRates.rate_equation_string"},{"category":"method","location":"api/#EnzymeRates.rescale_parameter_values-Tuple{EnzymeRates.AbstractEnzymeMechanism, NamedTuple}","page":"API Reference","text":"rescale_parameter_values(m, params::NamedTuple; scale_k_to_kcat=1.0)\n\nRescale SS rate constants so that _kcat_forward(m, result) ≈ scale_k_to_kcat. Non-SS parameters (K's, Keq, E_total, L, regulatory K's) are unchanged.\n\n\n\n\n\n","title":"EnzymeRates.rescale_parameter_values"},{"category":"macro","location":"api/#EnzymeRates.@allosteric_mechanism-Tuple{Any}","page":"API Reference","text":"@allosteric_mechanism begin\n    substrates: F6P\n    products:   F16BP\n    catalytic_multiplicity: 2\n    allosteric_regulators: A::OnlyA, I::OnlyI\n\n    catalytic_steps: begin\n        E + F6P ⇌ E(F6P)        :: EqualAI\n        E(F6P) <--> E(F16BP)    :: EqualAI\n        (E(F16BP) ⇌ E + F16BP)  :: EqualAI\n    end\n\n    regulatory_site(multiplicity = 4): begin\n        ligands: A\n    end\n    regulatory_site(multiplicity = 4): begin\n        ligands: I\n    end\nend\n\nBuild an AllostericEnzymeMechanism (MWC, two conformations).\n\nsubstrates:, products:, catalytic_inhibitors: accept comma-separated bare symbols.\nallosteric_regulators: requires name::Tag per entry, where Tag is one of OnlyA, OnlyI, EqualAI, NonequalAI.\ncatalytic_multiplicity: N is the subunit count for the catalytic site (default 1).\ncatalytic_steps: begin ... end is required (exactly once); each step or parenthesized step-group must carry a ::Tag from the same set. Function- call species notation (E(F6P), Estar(B; residual = A - P)) is supported.\nregulatory_site(multiplicity = N): begin ligands: L1, L2 end declares one regulatory site per block with multiplicity N and the ligands listed inside. Each ligand must appear in allosteric_regulators:; a ligand may not appear in two sites. Ligands declared in allosteric_regulators: but not assigned to any regulatory_site(...): block default to a single-ligand site at the catalytic multiplicity.\n\n\n\n\n\n","title":"EnzymeRates.@allosteric_mechanism"},{"category":"macro","location":"api/#EnzymeRates.@enzyme_mechanism-Tuple{Any}","page":"API Reference","text":"@enzyme_mechanism begin\n    substrates: S\n    products:   P\n    regulators: I\n\n    steps: begin\n        E + S ⇌ E(S)                   # function-call species notation\n        (E(S) ⇌ E(P), E_alt(S) ⇌ E_alt(P))   # parenthesized → shared kinetics\n        E(S) + I ⇌ E(S, I)             # dead-end\n        E(P) ⇌ E + P\n    end\nend\n\nBuild a plain (non-allosteric) EnzymeMechanism.\n\nsubstrates:, products:, regulators: accept comma-separated bare symbols. Atom brackets (S[C]) are rejected at the mechanism level.\nregulators: entries are treated as CompetitiveInhibitors when later passed to EnzymeReaction.\nSame-kinetics groups are expressed via parenthesized step-groups; no constraints: block needed.\nAllosteric-only constructs (site(...) / ::Tag / allosteric_regulators: / catalytic_inhibitors:) are rejected.\n\nSpecies notation on step sides:\n\nBare Symbol that matches a declared metabolite → that metabolite.\nBare Symbol otherwise (e.g. E, Estar, ES) → conformation-only species named after the Symbol.\nE(S) / E(S, P) → species with conformation :E and bound metabolites; synthesized name is :E_<bound...> with bound names sorted alphabetically (matching name(::Species)).\nEstar(; residual = A - P) → species with empty bound and a residual recording +A / −P; synthesized name is :Estar_res_+A_-P.\nEstar(B; residual = A - P) → bound + residual; name :Estar_B_res_+A_-P.\n\nConformation labels cannot shadow declared metabolite names.\n\n\n\n\n\n","title":"EnzymeRates.@enzyme_mechanism"},{"category":"macro","location":"api/#EnzymeRates.@enzyme_reaction-Tuple{Any}","page":"API Reference","text":"@enzyme_reaction begin\n    substrates: S[C6H12O6], ATP[C10H16N5O13P3]\n    products:   P[C6H13O9P]\n    competitive_inhibitors: I            # bare OK; mults default to catalytic\n    allosteric_regulators: A(1, 2, 4)    # per-reg mults required\n    allowed_catalytic_multiplicities: (1, 2, 4)\n    # or shorthand:\n    # oligomeric_state: 2\nend\n\nEmit an EnzymeReaction.\n\nsubstrates: / products: — comma-separated entries with required atom brackets (S[C6H12O6]). Multi-atom forms like [C2,N] are allowed.\ncompetitive_inhibitors: / dead_end_inhibitors: — CompetitiveInhibitor entries (catalytic-site binding). May be bare I (multiplicities default to allowed_catalytic_multiplicities) or I(m1, m2, ...) to override.\nallosteric_regulators: — AllostericRegulator entries. Each entry must declare per-regulator multiplicities as A(m1, m2, ...) or a single value A(m). An entry may carry a type tag, A::Activator or A::Inhibitor; untagged entries default to :unspecified. Type tags are only valid on allosteric_regulators: entries.\nallowed_catalytic_multiplicities: — tuple of positive Ints (default (1,)).\noligomeric_state: N — shorthand for allowed_catalytic_multiplicities: (N,).\n\n\n\n\n\n","title":"EnzymeRates.@enzyme_reaction"},{"category":"method","location":"api/#EnzymeRates.AllostericEnzymeMechanism-Tuple{EnzymeRates.AllostericMechanism}","page":"API Reference","text":"AllostericEnzymeMechanism(am::AllostericMechanism)\n\nLift an AllostericMechanism to its singleton AllostericEnzymeMechanism type. The catalytic side becomes an EnzymeMechanism lifting through Mechanism(am.reaction, am.cat_steps). Catalytic and regulatory allosteric data are encoded directly into the type parameters.\n\n\n\n\n\n","title":"EnzymeRates.AllostericEnzymeMechanism"},{"category":"type","location":"api/#EnzymeRates.EnzymeMechanism","page":"API Reference","text":"EnzymeMechanism{Sig}\n\nSingleton type encoding an enzyme mechanism. Sig is the tuple (reaction_sig, steps_sig) produced by _sig_of(::Mechanism):\n\nreaction_sig encodes the EnzymeReaction (reactants, regulators, catalytic multiplicities) as a tuple of tuples of Symbols/Ints.\nsteps_sig encodes Vector{Vector{Step}} as a nested tuple where the outer level is kinetic groups and the inner level is Step data (from-species, to-species, bound metabolite, is-equilibrium).\n\nThe conversion functions are _sig_of and _mechanism_from_sig; the exact layout is internal — users construct via EnzymeMechanism(::Mechanism).\n\n\n\n\n\n","title":"EnzymeRates.EnzymeMechanism"},{"category":"type","location":"api/#EnzymeRates.EnzymeReaction","page":"API Reference","text":"EnzymeReaction\n\nThe public concrete reaction descriptor: substrates, products, optional regulators, and the set of catalytic multiplicities the mechanism enumerator is allowed to consider. It is the entry point to the package — pair it with rate data in an IdentifyRateEquationProblem, or pass it to EnzymeRates.init_mechanisms to enumerate candidate mechanisms.\n\nFields\n\nreactants::Vector{ReactantAtoms} — substrates and products, each carrying its per-atom inventory (used for ping-pong residual bookkeeping and atom conservation across steps).\nregulators::Vector{RegulatorMults} — competitive inhibitors and allosteric regulators, each with its allowed oligomeric multiplicities.\nallowed_catalytic_multiplicities::Vector{Int} — the oligomeric states the allosteric enumerator may assign to the catalytic core.\n\nConstruct one with the @enzyme_reaction DSL. Each reactant takes an atom bracket (S[C], A[C1H1]); the brackets are load-bearing for ping-pong and multi-substrate reactions. Reactants and regulators are sorted by name in the constructor, so two equivalent declarations compare equal under ==/hash.\n\njulia> using EnzymeRates\n\njulia> rxn = @enzyme_reaction begin\n           substrates: S[C]\n           products:   P[C]\n       end;\n\njulia> rxn isa EnzymeReaction\ntrue\n\njulia> rxn\nEnzymeReaction: S ⇌ P\n\n\n\n\n\n","title":"EnzymeRates.EnzymeReaction"},{"category":"type","location":"api/#EnzymeRates.FittingProblem","page":"API Reference","text":"FittingProblem{M, D}\n\nHolds pre-processed experimental data and mechanism info for fitting rate constants.\n\nFields\n\nmechanism: an AbstractEnzymeMechanism instance\ndata: NamedTuple of column vectors (via Tables.columntable)\ngroup_point_indexes: row indices grouped by unique group values\nKeq: fixed equilibrium constant\nscale_k_to_kcat: a positive Real selects relative mode (per-group-centered loss); nothing selects absolute per-enzyme-turnover mode (uncentered loss)\nlog_abs_rates: pre-computed log.(abs.(Rate))\nlog_ratios_buffer: pre-allocated working buffer for loss computation\n\n\n\n\n\n","title":"EnzymeRates.FittingProblem"},{"category":"method","location":"api/#EnzymeRates.FittingProblem-Tuple{EnzymeRates.AbstractEnzymeMechanism, Any}","page":"API Reference","text":"FittingProblem(mechanism::AbstractEnzymeMechanism, table;\n    Keq::Real, scale_k_to_kcat::Union{Real,Nothing}=1.0)\n\nConstruct a FittingProblem from an enzyme mechanism and tabular data.\n\nThe table must have columns: group, Rate, and one column per metabolite matching metabolites(mechanism). Uses Tables.columntable for conversion.\n\nscale_k_to_kcat selects the loss mode: a positive Real (default 1.0) treats the data as relative (per-group-centered loss); nothing treats it as absolute per-enzyme turnover (uncentered loss).\n\nRate values must be nonzero (zero rates produce -Inf in log space).\n\n\n\n\n\n","title":"EnzymeRates.FittingProblem"},{"category":"type","location":"api/#EnzymeRates.IdentifyRateEquationProblem","page":"API Reference","text":"IdentifyRateEquationProblem{R, D}\n\nHolds the reaction, experimental data, and equilibrium constant for rate equation identification.\n\nFields\n\nreaction: the EnzymeReaction instance\ndata: NamedTuple of column vectors with :group, :Rate, and metabolite columns\nKeq: fixed equilibrium constant\nscale_k_to_kcat: target kcat for SS-rate rescaling before fitting (nothing = no rescaling, positive Float64 = target)\n\n\n\n\n\n","title":"EnzymeRates.IdentifyRateEquationProblem"},{"category":"type","location":"api/#EnzymeRates.IdentifyRateEquationResults","page":"API Reference","text":"IdentifyRateEquationResults\n\nResults from identify_rate_equation.\n\nFields\n\nbest: the best AbstractEnzymeMechanism (lowest loss at optimal param count)\ncv_results: DataFrame with LOOCV results for top candidates per param count\n\n\n\n\n\n","title":"EnzymeRates.IdentifyRateEquationResults"},{"category":"constant","location":"api/#EnzymeRates.Full","page":"API Reference","text":"Singleton instance for full (raw) mode.\n\n\n\n\n\n","title":"EnzymeRates.Full"},{"category":"constant","location":"api/#EnzymeRates.Reduced","page":"API Reference","text":"Singleton instance for reduced (Haldane-Wegscheider) mode.\n\n\n\n\n\n","title":"EnzymeRates.Reduced"},{"category":"section","location":"deriving/regulators/#Mechanisms-with-regulators","page":"Mechanisms with regulators","text":"A regulator is a species that binds the enzyme and changes its rate without being consumed by the reaction. The derivation treats regulators no differently from substrates and products: it walks whatever enzyme-form graph the steps define and reads the rate law off it. A regulator's kind — competitive inhibitor, essential activator, general modifier — is therefore not a setting you choose but an emergent consequence of where it binds and how the kinetics of regulator-bound enzyme species change.\n\nThis makes the derivation more general than the automatic search. When identify_rate_equation enumerates mechanisms it adds only competitive (dead-end) inhibitors and allosteric regulators. A hand-written @enzyme_mechanism, though, can express the full range — competitive, non-competitive, and uncompetitive inhibition, essential and non-essential activation — simply by choosing which enzyme forms the regulator binds. Declare the regulator with regulators: <name> and write its binding steps.","title":"Mechanisms with regulators"},{"category":"section","location":"deriving/regulators/#A-competitive-inhibitor","page":"Mechanisms with regulators","text":"A competitive inhibitor binds the free enzyme only, competing with substrate. The inhibitor-bound form has no catalytic exit, so it appears only in the denominator — it ties up enzyme but produces no flux:\n\nusing EnzymeRates\ncompetitive = @enzyme_mechanism begin\n    substrates: S\n    products:   P\n    regulators: I\n    steps: begin\n        E + S ⇌ E(S)\n        E(S) <--> E(P)\n        E(P) ⇌ E + P\n        E + I ⇌ E(I)\n    end\nend\nprint(rate_equation_string(competitive))\n\nThe inhibitor adds a single I / K_Iinh_E term to the denominator and leaves the numerator untouched — the textbook competitive form. (I renders with an inh marker, K_Iinh_E, because it occupies a regulator binding site.)","title":"A competitive inhibitor"},{"category":"section","location":"deriving/regulators/#An-essential-activator","page":"Mechanisms with regulators","text":"An essential activator must be bound for catalysis to occur at all: there is no catalytic cycle on the activator-free enzyme. The activator R binds the free enzyme, and every catalytic form carries it:\n\nessential = @enzyme_mechanism begin\n    substrates: S\n    products:   P\n    regulators: R\n    steps: begin\n        E + R ⇌ E(R)\n        E(R) + S ⇌ E(S, R)\n        E(S, R) <--> E(P, R)\n        E(R) + P ⇌ E(P, R)\n    end\nend\nprint(rate_equation_string(essential))\n\nNow the activator concentration R appears in every numerator term, so the rate vanishes as R → 0 — the defining behaviour of an essential activator, opposite to the competitive inhibitor whose regulator stayed in the denominator. Nothing in the DSL marks R as an activator; its role follows entirely from binding before the substrate and riding along through catalysis.\n\nSee also: @enzyme_mechanism, rate_equation_string.","title":"An essential activator"},{"category":"section","location":"deriving/kinetic_groups/#Kinetic-groups","page":"Kinetic groups","text":"A kinetic group is a set of steps that share one kinetic parameter. The point is parsimony: when several steps are chemically the same — the same metabolite binding the enzyme, or the same chemical conversion happening in more than one place — there is no reason for each to carry its own constant. Putting them in one group makes the model say they behave identically, and the rate equation then carries a single parameter for the whole set instead of one per step.\n\nBy default @enzyme_mechanism keeps each step in its own group. To declare that several steps share a parameter, wrap them in parentheses — a parenthesized step-group binds the steps into one kinetic group:\n\nsteps: begin\n    (E + Q ⇌ E(Q), E(A) + Q ⇌ E(A, Q))   # both Q-binding steps share one constant\n    ...\nend\n\nThis is a modeling choice, distinct from the thermodynamic reduction (Thermodynamic constraints). The reduction also removes parameters, but only when a Haldane or Wegscheider relation forces two constants to be equal. Kinetic grouping is a decision you make: it ties together steps the thermodynamics leaves independent.","title":"Kinetic groups"},{"category":"section","location":"deriving/kinetic_groups/#An-example:-a-two-substrate,-two-product-mechanism","page":"Kinetic groups","text":"Consider a random-order reaction A + B ⇌ P + Q with two abortive complexes: E(A, Q), where the enzyme binds substrate A together with the product Q of the other half-reaction, and E(B, P), its mirror. Like the catalytic complexes, each abortive complex forms either way — E(A, Q) by Q binding E(A) or A binding E(Q). Every metabolite therefore binds the enzyme in several places: A binds free E, the E(B) complex, and E(Q), and the same holds for B, P, and Q.\n\nWith every step in its own group, each of those bindings gets its own constant:\n\nusing EnzymeRates\nungrouped = @enzyme_mechanism begin\n    substrates: A, B\n    products:   P, Q\n    steps: begin\n        E + A ⇌ E(A)\n        E + B ⇌ E(B)\n        E(A) + B ⇌ E(A, B)\n        E(B) + A ⇌ E(A, B)\n        E(A, B) <--> E(P, Q)\n        E(P, Q) ⇌ E(P) + Q\n        E(P, Q) ⇌ E(Q) + P\n        E(P) ⇌ E + P\n        E(Q) ⇌ E + Q\n        E(A) + Q ⇌ E(A, Q)\n        E(Q) + A ⇌ E(A, Q)\n        E(B) + P ⇌ E(B, P)\n        E(P) + B ⇌ E(B, P)\n    end\nend\nprint(rate_equation_string(ungrouped))\n\nThere are nine independent constants, with form-specific names: K_A_E, K_A_EB, and K_A_EQ for A on three different forms, and similar families for the others. Four more bindings are not fit at all but fixed by Wegscheider relations (K_B_EA, K_P_EB, K_Q_EA, K_Q_EP), since each catalytic and abortive loop closes a thermodynamic cycle.\n\nNow group every binding of a given metabolite together — all A-binding steps in one group, all B-binding in another, and likewise for P and Q. Each metabolite then has a single binding constant:\n\ngrouped = @enzyme_mechanism begin\n    substrates: A, B\n    products:   P, Q\n    steps: begin\n        (E + A ⇌ E(A), E(B) + A ⇌ E(A, B), E(Q) + A ⇌ E(A, Q))\n        (E + B ⇌ E(B), E(A) + B ⇌ E(A, B), E(P) + B ⇌ E(B, P))\n        E(A, B) <--> E(P, Q)\n        (E(P, Q) ⇌ E(Q) + P, E(P) ⇌ E + P, E(B) + P ⇌ E(B, P))\n        (E(P, Q) ⇌ E(P) + Q, E(Q) ⇌ E + Q, E(A) + Q ⇌ E(A, Q))\n    end\nend\nprint(rate_equation_string(grouped))\n\nThe parameter list collapses from nine constants to five — one binding constant per metabolite (K_A_E, K_B_E, K_P_E, K_Q_E) plus the catalytic k_EAB_to_EPQ:\n\n(ungrouped = parameters(ungrouped), grouped = parameters(grouped))\n\nThe denominator becomes symmetric: the two abortive complexes read A * Q / (K_A_E * K_Q_E) and B * P / (K_B_E * K_P_E), with no form-specific suffixes. The Wegscheider section is gone, too — once each metabolite has a single binding constant, the loop-closing relations become identities and drop out. Grouping did the collapsing the thermodynamics could not: it is what keeps the parameter count at the lowest physically meaningful value, and the mechanism enumeration starts there, splitting groups back apart only as the data warrant (see The enumeration engine).","title":"An example: a two-substrate, two-product mechanism"},{"category":"section","location":"identify/parallel/#Running-in-parallel","page":"Running in parallel","text":"A reaction as ordinary as two substrates and two products has more than 100,000 biochemically valid rate equations: the order substrates bind and products leave, the rapid-equilibrium-versus-steady-state choice at each step, and the dead-end complexes that can form all combine into a vast space. identify_rate_equation makes this tractable two ways. The rate-equation search keeps only the promising candidates at each parameter count instead of fitting the whole space, and the candidates it does fit run in parallel. This page covers the parallelism.\n\nThe search is embarrassingly parallel: every candidate equation is compiled and fit independently, and every leave-one-group-out cross-validation fold is independent too. identify_rate_equation runs both through Distributed.pmap. The same call scales from one core to a whole high-performance compute (HPC) cluster — you add worker processes and change nothing else. With no workers, pmap runs every fit on the main process, so the call already works on a laptop.","title":"Running in parallel"},{"category":"section","location":"identify/parallel/#Running-on-a-local-machine-with-several-cores","page":"Running in parallel","text":"Add local workers with addprocs, load the package and your optimizer on every worker with @everywhere, then load the data and run the search on the main process:\n\nusing Distributed, CSV\naddprocs(8)                                  # 8 local worker processes\n\n@everywhere using EnzymeRates, OptimizationCMAEvolutionStrategy\n\n# Read the data and build the problem on the main process. The workers receive\n# the problem automatically when the search distributes its fits to them.\ntable = CSV.File(\"rate_data.csv\")\ndata = (group = table.group, Rate = table.Rate,\n        A = table.A, B = table.B, P = table.P, Q = table.Q)   # one column per metabolite\n\nrxn = @enzyme_reaction begin\n    substrates: A[C], B[C]\n    products:   P[C], Q[C]\nend\nprob = IdentifyRateEquationProblem(rxn, data; Keq = 5.0)\n\nresults = identify_rate_equation(prob; optimizer = CMAEvolutionStrategyOpt())\n\n# Clean up the workers when the run finishes\nfor p in workers()\n    rmprocs(p)\nend\n\nThe @everywhere using line is required: each worker compiles and fits mechanisms on its own, so it needs both EnzymeRates and the optimizer package. The data file is read once on the main process and converted to a plain NamedTuple; pmap ships that problem to the workers with each batch of fits, so the workers need no data-loading packages. The main process collects the results and writes the CSV outputs to save_dir; the workers write nothing.","title":"Running on a local machine with several cores"},{"category":"section","location":"identify/parallel/#Running-on-a-compute-cluster-with-many-machines","page":"Running in parallel","text":"On a cluster, swap the addprocs line for a cluster manager that starts workers across the allocated nodes — the rest of the script is identical. For Slurm, SlurmClusterManager.jl reads the allocation and starts one worker per task. Save the script as identify.jl:\n\nusing Distributed, SlurmClusterManager, CSV\naddprocs(SlurmManager())                     # one worker per Slurm task\n\n@everywhere using EnzymeRates, OptimizationCMAEvolutionStrategy\n\ntable = CSV.File(\"rate_data.csv\")\ndata = (group = table.group, Rate = table.Rate,\n        A = table.A, B = table.B, P = table.P, Q = table.Q)\n\nrxn = @enzyme_reaction begin\n    substrates: A[C], B[C]\n    products:   P[C], Q[C]\nend\nprob = IdentifyRateEquationProblem(rxn, data; Keq = 5.0)\n\nresults = identify_rate_equation(prob; optimizer = CMAEvolutionStrategyOpt())\n\n# Clean up the workers when the run finishes\nfor p in workers()\n    rmprocs(p)\nend\n\nSubmit the script with sbatch: the batch script requests the allocation and launches Julia, and SlurmManager() then starts one worker per allocated task. This template uses juliaup to select the Julia version:\n\n#!/bin/bash\n# Job name:\n#SBATCH --job-name=YOUR_JOB_NAME\n#\n# Account:\n#SBATCH --account=YOUR_HPC_ACCOUNT\n#\n# Partition:\n#SBATCH --partition=YOUR_HPC_PARTITION_NAME\n#\n# Number of requested nodes:\n#SBATCH --nodes=24\n#\n# Processors per task:\n#SBATCH --cpus-per-task=1\n#\n# Wall clock limit:\n#SBATCH --time=24:00:00\n\n# Load software\nmodule purge\n\n# Run Julia script\n/PATH/TO/USER/.juliaup/bin/julia YOUR_IDENTIFY_RATE_EQUATION_CODE.jl\n\nSave this batch script with a .slurm extension and submit it with sbatch identify.slurm. Replace the placeholders with your account, partition, and paths, and point the last line at your Julia script — run Julia in the project where EnzymeRates and your optimizer are installed (add --project=/path/to/project if it is not the default). The worker count follows the allocation, so widening the search means requesting more nodes. Other schedulers work the same way through ClusterManagers.jl (PBS, SGE, LSF) or MPIClusterManagers.jl (MPI); only the addprocs line changes.","title":"Running on a compute cluster with many machines"},{"category":"section","location":"deriving/mwc_allostery/#Mechanisms-with-allosteric-regulators","page":"Mechanisms with allosteric regulators","text":"Allosteric regulation is one of the main ways metabolic enzymes are controlled. An allosteric regulator binds a regulatory site that is distinct from the catalytic site and, from there, either inhibits or activates the enzyme. A classical description of how this works is the Monod–Wyman–Changeux (MWC) model (Monod et al., 1965), and EnzymeRates can derive rate equations under that model.\n\nIn the MWC picture the enzyme exists in two conformations — an active state (A) and an inactive state (I) — in equilibrium with one another. (In the classic MWC notation A is the R state and I is the T state; the package uses A/I throughout, so K_A_… names correspond to the literature's R-state constants.) A coupling constant L sets the inactive-to-active population ratio of the free enzyme. What distinguishes an activator from an inhibitor is simply which conformation it prefers: an activator binds the active state more tightly and shifts the population toward A, while an inhibitor binds the inactive state and shifts it toward I. That preference is expressed entirely through the regulator's binding constants for the two states — its K_A versus its K_I.","title":"Mechanisms with allosteric regulators"},{"category":"section","location":"deriving/mwc_allostery/#Setting-up-an-MWC-mechanism","page":"Mechanisms with allosteric regulators","text":"Use @allosteric_mechanism, tagging each catalytic step and each regulator with its allosteric-state behavior. The printed rate equation shows the result:\n\nusing EnzymeRates\nallo = @allosteric_mechanism begin\n    substrates: S\n    products:   P\n    catalytic_multiplicity: 2\n    allosteric_regulators: A::OnlyA, I::OnlyI\n    catalytic_steps: begin\n        E + S ⇌ E(S)        :: EqualAI\n        E(S) <--> E(P)       :: NonequalAI\n        E(P) ⇌ E + P        :: EqualAI\n    end\n    regulatory_site(multiplicity = 2): begin\n        ligands: A\n    end\n    regulatory_site(multiplicity = 2): begin\n        ligands: I\n    end\nend\nprint(rate_equation_string(allo))\n\nThe independent parameters the mechanism fits:\n\nparameters(allo)\n\nEach catalytic kinetic group and each regulator carries one of four tags that controls how its A- and I-state symbols are rendered:\n\nTag Meaning Example symbol\n:EqualAI One shared symbol for both states K_S_E\n:NonequalAI Independent A and I symbols k_A_ES_to_EP, k_I_ES_to_EP\n:OnlyA Present in the active state only; zeroed in the inactive polynomial K_A_Areg\n:OnlyI Present in the inactive state only; zeroed in the active polynomial K_I_Ireg\n\nIn the example the two binding steps are :EqualAI, so they share K_S_E and K_P_E with no A/I token; the isomerization step is :NonequalAI, giving independent k_A_ES_to_EP and k_I_ES_to_EP; the activator A is :OnlyA (K_A_Areg, active state only) and the inhibitor I is :OnlyI (K_I_Ireg, inactive state only). L is the free-enzyme coupling constant. A catalytic step can never be :OnlyI — the active state must always be able to catalyze — so tagging one that way is a hard constructor error.\n\nThe mechanism also declares multiplicities, which capture the enzyme's oligomeric state. catalytic_multiplicity is the number of identical catalytic subunits; it becomes the exponent on each conformation's catalytic partition function. Each regulatory_site(multiplicity = m) says how many copies of that site the oligomer carries, raising the site's binding factor to the power m. In the example both are 2 — a homodimer with two copies of each regulatory site.\n\nnote: Note\nThe derivation reports the rate per catalytic site: catalytic_multiplicity enters only through the partition-function exponents, never as an overall multiplier on the numerator.","title":"Setting up an MWC mechanism"},{"category":"section","location":"deriving/mwc_allostery/#Derivation-of-the-MWC-rate-equation","page":"Mechanisms with allosteric regulators","text":"The MWC rate equation is assembled from two ordinary single-conformation rate equations — one for the active state, one for the inactive state — combined by a partition function. The derivation rests on two rapid-equilibrium assumptions. Allosteric regulators bind fast relative to catalysis. And the enzyme changes conformation only while free: it settles into the active or inactive state before it binds anything — the free-enzyme inactive-to-active ratio is L — and keeps that conformation for a full catalytic cycle. The overall rate is then each conformation's catalytic cycle weighted by its free-enzyme population.\n\nAn equivalent statement lets every form interconvert between the A and I states, but with a thermodynamically fixed ratio: L for the free enzyme, and L scaled by the form's active-to-inactive binding-constant ratio for a bound form (so L itself for an :EqualAI ligand). The two statements agree except when catalysis differs between the conformations (:NonequalAI); there, committing to one conformation per cycle — the model here — routes each molecule's turnover through a single conformation rather than blending the two, a difference of a few percent.\n\nTreating regulator binding as steady state instead would not help — a regulator is not consumed, so a non-RE binding step contributes only the ratio of its on- and off-rate constants, which is exactly the equilibrium constant the RE treatment already uses, and the two rates would not be separately identifiable from rate data.\n\nFor a mechanism with catalytic multiplicity cat_n, the package normalizes each conformation to its own free-enzyme population and combines the two:\n\nv = E_total * num / den\nnum = (N_A/D_A) * (Q_A/D_A)^(cat_n-1) * W_A + L * (N_I/D_I) * (Q_I/D_I)^(cat_n-1) * W_I\nden = (Q_A/D_A)^cat_n * W_A                 + L * (Q_I/D_I)^cat_n                 * W_I\n\nN_A, Q_A are the active state's catalytic numerator and partition function (the King–Altman/Cha polynomials for the catalytic cycle), and D_A is the spanning-tree weight of its free-enzyme segment. N_I, Q_I, D_I are the inactive-state counterparts, derived the same way from the inactive-state graph — :OnlyA groups pruned, :EqualAI groups keeping the shared active-state constants, and :NonequalAI groups carrying their own I-state names. W_A, W_I are products of the regulatory-site factors (1 + lig/K)^m, and L weights the inactive branch throughout.\n\nThe free-enzyme weight D is 1 whenever the catalytic graph is a single rapid-equilibrium segment — the common case, where Q/D = Q and the equation is the textbook MWC form. D differs from 1 only when an :OnlyA/:OnlyI binding fragments the graph or a steady-state binding splits it into segments; the normalization then keeps the two conformations on a common free-enzyme basis, which is what stops a bare rate constant from leaking into the L-term. The package divides Q/D directly when D is a single rate constant — giving the readable 1 + [S]/K + … form — and clears the fraction to a polynomial when D carries a metabolite. When a catalytic group is :OnlyA, the inactive cycle cannot close, so the package sets N_I = 0: the inactive branch still contributes to the denominator as enzyme mass but carries no forward flux.\n\nIn the printed equation above cat_n = 2, so each partition function is squared: the active branch carries (1 + A / K_A_Areg) ^ 2 and a trivial 1 ^ 2 from the inhibitor site (absent in the active state), and the inactive branch mirrors it with (1 + I / K_I_Ireg) ^ 2.","title":"Derivation of the MWC rate equation"},{"category":"section","location":"deriving/mwc_allostery/#Thermodynamic-constraints-of-MWC-equations","page":"Mechanisms with allosteric regulators","text":"A single conformation already carries thermodynamic constraints: every cycle of steps must agree with the overall equilibrium constant Keq (a Haldane relation), and every cycle of binding steps must be path-independent (a Wegscheider relation). The derivation reports both and expresses the dependent rate constants through the independent ones. An MWC equation stacks two such equations, one per conformation, adding constraints neither has alone. Here we review several examples of such additional constraints.\n\nTag a lone substrate binding :NonequalAI while catalysis and product release stay :EqualAI, and the split collapses:\n\ncollapse = @allosteric_mechanism begin\n    substrates: S\n    products:   P\n    catalytic_multiplicity: 2\n    allosteric_regulators: R::OnlyI\n    catalytic_steps: begin\n        E + S ⇌ E(S)      :: NonequalAI\n        E(S) <--> E(P)    :: EqualAI\n        E(P) ⇌ E + P      :: EqualAI\n    end\n    regulatory_site(multiplicity = 2): begin\n        ligands: R\n    end\nend\nprint(rate_equation_string(collapse))\n\nThe equation reports K_I_S_E = K_A_S_E. The Haldane relation fixes Keq from the catalytic rate constants and the two affinities in each conformation; with catalysis, product release, and Keq all shared, the substrate affinity is pinned as well. The tag asked for a difference thermodynamics forbids, so the substrate stops distinguishing the states.\n\nTag both the substrate and product bindings :NonequalAI, and one degree of freedom survives:\n\ncoupled = @allosteric_mechanism begin\n    substrates: S\n    products:   P\n    catalytic_multiplicity: 2\n    allosteric_regulators: R::OnlyI\n    catalytic_steps: begin\n        E + S ⇌ E(S)      :: NonequalAI\n        E(S) <--> E(P)    :: EqualAI\n        E(P) ⇌ E + P      :: NonequalAI\n    end\n    regulatory_site(multiplicity = 2): begin\n        ligands: R\n    end\nend\nprint(rate_equation_string(coupled))\n\nNow K_I_P_E = K_A_P_E · K_I_S_E / K_A_S_E, i.e. K_P^I / K_S^I = K_P^A / K_S^A: the affinities differ freely as long as they differ together, holding their ratio fixed. This is the thermodynamically consistent form of a K-system, and it needs two coupled :NonequalAI bindings, not one.\n\nOften a K-system uses exclusive :OnlyA binding: the substrate binds the active state alone, and the inactive cycle is pruned (N_I = 0). Its catalytic step must be :OnlyA too. This is a thermodynamic requirement, not a convention: the two conformations share Keq, so a ligand that binds only the active state (K_I → ∞) forces the inactive catalytic rate to zero, and an :EqualAI or :NonequalAI catalytic tag would assert a rate the Haldane relation forbids. The constructor rejects the inconsistent combination.\n\nThe exception is a balanced K-system. When :OnlyA binds both a substrate and a product, the two K_I → ∞ limits cancel in the Haldane relation: the affinities diverge together and their ratio stays free, so :EqualAI catalysis is legal.\n\nA steady-state binding splits the constraint further, carrying an affinity (kon/koff) the cycles constrain and a speed (kon·koff) they do not: a forbidden affinity collapses while the speed stays free, so the two conformations bind with the same Kd but different kinetics. Tagging the substrate binding steady-state and :NonequalAI derives its reverse rate (koff_I_S_E = koff_A_S_E · kon_I_S_E / kon_A_S_E) and leaves the forward rate a free parameter:\n\nss = @allosteric_mechanism begin\n    substrates: S\n    products:   P\n    catalytic_multiplicity: 2\n    allosteric_regulators: R::OnlyI\n    catalytic_steps: begin\n        E + S <--> E(S)    :: NonequalAI\n        E(S) <--> E(P)     :: EqualAI\n        E(P) ⇌ E + P       :: EqualAI\n    end\n    regulatory_site(multiplicity = 2): begin\n        ligands: R\n    end\nend\nparameters(ss)","title":"Thermodynamic constraints of MWC equations"},{"category":"section","location":"deriving/mwc_allostery/#See-also","page":"Mechanisms with allosteric regulators","text":"@allosteric_mechanism — DSL reference\nparameters — list independent parameter names\nrate_equation_string — inspect the derived equation","title":"See also"},{"category":"section","location":"deriving/ping_pong/#Ping-pong-mechanisms","page":"Ping-pong mechanisms","text":"In a ping-pong (double-displacement) mechanism the enzyme carries a covalent fragment between two half-reactions. The first substrate binds and transfers part of its atoms to the enzyme; the modified enzyme releases the first product, then binds the second substrate and completes the transfer to form the second product. The enzyme returns to its original form only at the end of the full cycle.","title":"Ping-pong mechanisms"},{"category":"section","location":"deriving/ping_pong/#A-ping-pong-bi-bi-example","page":"Ping-pong mechanisms","text":"A covalently modified enzyme form is written by giving the enzyme a residual = … keyword — an arithmetic expression over the declared metabolites that lists the atoms it has gained (with +) and is committed to release (with -). Here substrate A binds, product P leaves a modified enzyme E(; residual = A - P) carrying A's atoms, substrate B binds the modified form, and product Q leaves to regenerate free E:\n\nusing EnzymeRates\nm = @enzyme_mechanism begin\n    substrates: A, B\n    products:   P, Q\n    steps: begin\n        E + A <--> E(A)\n        E(A) <--> E(; residual = A - P) + P\n        E(; residual = A - P) + B <--> E(B; residual = A - P)\n        E(B; residual = A - P) <--> E + Q\n    end\nend\nprint(rate_equation_string(m))\n\nThe derived equation has the signature of a ping-pong mechanism: the numerator is the difference A·B − P·Q, and the denominator has no constant term. A sequential mechanism always carries a 1 in its denominator for the free enzyme; a ping-pong enzyme never sits idle as free E during turnover — it is always either loaded with substrate or carrying the covalent residual — so that constant term is absent. The residual-bearing forms take descriptive names such as E_res_+A_-P (the modified free enzyme) and EB_res_+A_-P (with B additionally bound).","title":"A ping-pong bi-bi example"},{"category":"section","location":"deriving/iso/#Iso-mechanisms","page":"Iso mechanisms","text":"In an iso mechanism the enzyme passes through two distinct conformations during a single turnover. The substrate binds one conformation; catalysis converts the enzyme–substrate complex into the other conformation of the enzyme–product complex; the product leaves from that conformation; and the free enzyme must then isomerize back to the first conformation before it can bind substrate again. That free-enzyme isomerization — the \"iso\" step — is the defining feature.","title":"Iso mechanisms"},{"category":"section","location":"deriving/iso/#An-iso-uni-uni-example","page":"Iso mechanisms","text":"A second enzyme conformation is written with a different conformation label — Eprime in place of E. Bound forms carry their conformation (Eprime(P)), and the free enzyme moves between conformations through an isomerization step such as Eprime <--> E. Here S binds E, catalysis turns E(S) into Eprime(P), P leaves Eprime, and the free enzyme isomerizes from Eprime back to E:\n\nusing EnzymeRates\nm = @enzyme_mechanism begin\n    substrates: S\n    products:   P\n    steps: begin\n        E + S ⇌ E(S)\n        E(S) <--> Eprime(P)\n        Eprime(P) ⇌ Eprime + P\n        Eprime <--> E\n    end\nend\nprint(rate_equation_string(m))\n\nThe derived equation carries the signature of an iso mechanism: an S·P cross term in the denominator (the … P * S / (K_P_Eprime * K_S_E) terms above). An ordinary single-conformation Michaelis–Menten enzyme does not have an S·P term. Saturating an iso enzyme with substrate and product together can therefore slow turnover in a way a regular Michaelis–Menten enzyme cannot. Each conformation appears in the parameter names: K_S_E is S binding to E, K_P_Eprime is P binding to Eprime, and k_E_to_Eprime / k_Eprime_to_E are the isomerization rate constants.","title":"An iso uni-uni example"},{"category":"section","location":"fitting/tutorial/#Fitting-tutorial-and-data-format","page":"Fitting tutorial & data format","text":"This page shows how to fit a rate equation to experimental data from start to finish. It covers the data format, constructing a FittingProblem, running the multi-start optimizer, and reading the result.","title":"Fitting tutorial & data format"},{"category":"section","location":"fitting/tutorial/#Data-format","page":"Fitting tutorial & data format","text":"FittingProblem accepts any Tables.jl-compatible source — a DataFrame, a CSV-loaded table, or a plain NamedTuple of equal-length vectors. The required columns are:\n\nColumn Type Description\ngroup any One independent experiment — rates measured with the same amount of enzyme at varying metabolite concentrations (a single kinetic-data figure from a paper is a typical group). Leave-one-group-out cross-validation folds on this column.\nRate nonzero Real Measured reaction rate. Must be nonzero — the loss works in log space.\none per metabolite Real Concentration of each metabolite, in molar (M) — use M for every metabolite so the fitted kinetic constants stay interpretable. Column names must match metabolites(mechanism) exactly.\n\nCall metabolites(mechanism) to find which concentration columns your data needs before constructing the problem.\n\nnote: No `E_total` column\nThe fitter evaluates every prediction at E_total = 1, comparing your Rate against the per-enzyme turnover — in effect it fits Rate / E_total. The default relative mode needs no action here: per-group centering cancels each group's enzyme amount. For absolute mode (scale_k_to_kcat = nothing), first divide each rate by the enzyme amount used in its group. See Normalized vs absolute rate for the full explanation.\n\nusing EnzymeRates\n\nuni_uni = @enzyme_mechanism begin\n    substrates: S\n    products:   P\n    steps: begin\n        E + S <--> E(S)\n        E(S) <--> E + P\n    end\nend\n\nmetabolites(uni_uni)\n\nA missing required column, a missing metabolite column, or a zero Rate each raises an ErrorException at construction — the check runs before any fitting.","title":"Data format"},{"category":"section","location":"fitting/tutorial/#Building-the-FittingProblem","page":"Fitting tutorial & data format","text":"The example below generates synthetic data by evaluating the true rate equation on a concentration grid, then wraps it in a FittingProblem.\n\n# Independent fitted params for the reduced equation: koff_P_ES, kon_P_ES, kon_S_E\ntrue_params = (koff_P_ES = 3.0, kon_P_ES = 5.0, kon_S_E = 10.0, Keq = 2.0, E_total = 1.0)\n\nconcs_list = [(S = s, P = p) for s in (0.5, 1.0, 2.0, 5.0, 10.0) for p in (0.1, 0.5)]\n\ndata = (\n    group = fill(\"G1\", length(concs_list)),\n    Rate  = [rate_equation(uni_uni, c, true_params) for c in concs_list],\n    S     = [c.S for c in concs_list],\n    P     = [c.P for c in concs_list],\n)\n\nfp = FittingProblem(uni_uni, data; Keq = 2.0)\n\nKeq, the equilibrium constant of the overall reaction (the product-to-substrate concentration ratio at equilibrium, fixed by reaction thermodynamics), is a required keyword argument and is always user-supplied — the package never estimates it from data. For most enzyme reactions Keq is known — measured directly, or computed from a resource such as eQuilibrator. The constructor also accepts a concrete Mechanism or AllostericMechanism and compiles it once at construction, so the fitting hot path pays no compilation overhead.","title":"Building the FittingProblem"},{"category":"section","location":"fitting/tutorial/#Running-the-fit","page":"Fitting tutorial & data format","text":"fit_rate_equation requires an explicit optimizer — the base package depends only on Optimization.jl and ships no solver backend. Install OptimizationCMAEvolutionStrategy (the recommended choice; a pure-Julia CMA-ES) or OptimizationBBO (a pure-Julia differential-evolution alternative) and pass its optimizer object. See Loss & optimizers for details.\n\nusing OptimizationCMAEvolutionStrategy\n\nresult = fit_rate_equation(\n    fp, CMAEvolutionStrategyOpt();\n    n_restarts = 3, maxtime = 10.0,\n)\n\nkeys(result)\n\nresult.retcode isa Symbol\n\nresult.params\n\nThe fit runs n_restarts independent optimizations from random initial points and returns the best. Multiple restarts are necessary because finding the global minimum of an enzyme rate-equation fit is NP-hard — any single optimization can settle in a local minimum — and because the optimizer is stochastic, result.params and result.loss vary from run to run. Increase n_restarts until the fit returns the same loss on every run; empirically, 10–20 restarts is enough for many complex rate equations.\n\nCheck result.retcode === :Success to confirm the optimizer converged on its own criteria. Any other value — :Default, :MaxTime, :Failure, :NoFiniteLoss — means the fit is un-converged. When time or restart budgets are tight, :MaxTime is common; increase n_restarts and maxtime for production fits.\n\nThe returned params reflect kcat normalization: with the default scale_k_to_kcat = 1.0, the SS rate constants are rescaled so that kcat = 1. Normalized vs absolute rate explains scale_k_to_kcat; Loss & optimizers covers the loss function and how to supply your own optimizer.","title":"Running the fit"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"This page walks through the three pillars of EnzymeRates.jl in one arc: define a mechanism, derive its rate equation, fit it to data, and identify the best mechanism from data automatically. Each section links forward to the full pillar page for details.\n\n","title":"Getting Started"},{"category":"section","location":"getting_started/#Define-a-reaction-and-a-mechanism","page":"Getting Started","text":"A reaction names the substrates, products, and any regulators. A mechanism adds the elementary steps.\n\nusing EnzymeRates\n\nrxn = @enzyme_reaction begin\n    substrates: S[C]\n    products:   P[C]\nend\n\nm = @enzyme_mechanism begin\n    substrates: S\n    products:   P\n    steps: begin\n        E + S ⇌ E(S)\n        E(S) <--> E(P)\n        E(P) ⇌ E + P\n    end\nend\n\nm isa EnzymeMechanism\n\n⇌ marks a rapid-equilibrium binding step, described by one equilibrium constant K. <--> marks a steady-state interconversion, described by independent forward and reverse rate constants. The result is an EnzymeMechanism value. The expression m isa EnzymeMechanism (rather than typeof(m)) is shown deliberately: the Sig type parameter that encodes the mechanism structure is an unreadable string, so isa is the useful check.\n\n","title":"Define a reaction and a mechanism"},{"category":"section","location":"getting_started/#Derive-the-rate-equation","page":"Getting Started","text":"Derivation tutorial and the other deriving pages cover all mechanism forms in depth.\n\nparameters(m)\n\nmetabolites(m)\n\nprint(rate_equation_string(m))\n\nparameters(m) returns the independent parameter symbols. metabolites(m) returns the concentration symbols the rate equation expects. Keq is the equilibrium constant of the overall reaction — the ratio of product to substrate concentrations once the reaction has reached equilibrium, fixed by the reaction's thermodynamics. It is always user-supplied; the package never estimates it from data.\n\n","title":"Derive the rate equation"},{"category":"section","location":"getting_started/#Evaluate-the-rate-equation-numerically","page":"Getting Started","text":"Supply a NamedTuple of parameters and a NamedTuple of concentrations:\n\nparams = (\n    K_S_E = 1.0,\n    K_P_E = 1.0,\n    k_ES_to_EP = 10.0,\n    Keq = 2.0,\n    E_total = 1.0,\n)\nconcs = (S = 2.0, P = 0.5)\n\nrate_equation(m, concs, params)\n\n","title":"Evaluate the rate equation numerically"},{"category":"section","location":"getting_started/#Estimate-kinetic-parameters-from-data","page":"Getting Started","text":"Fitting tutorial & data format covers the data format, loss function, and optimizer options in depth.\n\nGenerate noiseless synthetic data from the mechanism above:\n\nusing OptimizationCMAEvolutionStrategy, Random\nRandom.seed!(1)\n\ngroups = String[]; Rate = Float64[]; Svals = Float64[]; Pvals = Float64[]\nfor g in 1:3, _ in 1:8\n    s = 0.1 + 9.9 * rand()\n    p = 0.1 + 9.9 * rand()\n    push!(groups, \"G$g\")\n    push!(Rate, rate_equation(m, (S = s, P = p), params))\n    push!(Svals, s); push!(Pvals, p)\nend\ndata = (group = groups, Rate = Rate, S = Svals, P = Pvals)\n\nfp = FittingProblem(m, data; Keq = 2.0)\nfitted_params, loss, retcode = fit_rate_equation(fp, CMAEvolutionStrategyOpt(); n_restarts = 1, maxtime = 2.0)\nretcode\n\nThe data table has a group column identifying measurement batches that share one E_total, a Rate column with measured rates, and one column per metabolite or regulator name (here S and P). Keq is a required keyword, always user-supplied.\n\n","title":"Estimate kinetic parameters from data"},{"category":"section","location":"getting_started/#Identify-the-enzyme-mechanism-from-data","page":"Getting Started","text":"Identify tutorial covers the full rate-equation search, cross-validation, and production settings.\n\nBuild an IdentifyRateEquationProblem from the reaction and data, then run identify_rate_equation with the width-1 beam settings so the search finishes in seconds:\n\nprob = IdentifyRateEquationProblem(rxn, data; Keq = 2.0)\n\nresults = identify_rate_equation(prob;\n    min_beam_width = 1,\n    loss_rel_threshold = 1.0,\n    loss_abs_threshold = 0.0,\n    max_param_count = 6,\n    n_cv_candidates = 1,\n    save_dir = mktempdir(),\n    optimizer = CMAEvolutionStrategyOpt(),\n    n_restarts = 1, maxtime = 1.0,\n    show_progress = false)\n\nprint(rate_equation_string(results.best))\n\nmin_beam_width=1 and loss_rel_threshold=1.0 collapse the beam to exactly one survivor per parameter-count level, making the search fast and deterministic. The full production search uses the wider defaults (min_beam_width=50, loss_rel_threshold=2.0, loss_abs_threshold=0.01, loss_parsimony_threshold=1.01, max_param_count=20, eq_complexity_filter=337) and would often run for many hours and require a High Performance Compute cluster (see Running in parallel). save_dir is mandatory; the search writes its progress and results there: progress.log, initial_mechanisms.csv, one equation_search_iteration_N.csv per beam iteration, and — once cross-validation finishes — loocv_results.csv (the full leave-one-group-out table for every candidate that entered CV) and best_equation.csv (the single selected equation with its fitted parameters). The last two persist the model-selection outcome, so a cluster run's result is available without re-deriving it from the iteration files.","title":"Identify the enzyme mechanism from data"},{"category":"section","location":"deriving/thermodynamic_constraints/#Thermodynamic-constraints","page":"Thermodynamic constraints","text":"Every reversible mechanism contains cycles: sequences of steps that return the enzyme to its starting form. Thermodynamics constrains the rate constants around each cycle — their product must equal a fixed function of the overall equilibrium constant. The package identifies these constraints automatically and removes one rate constant per independent cycle, reducing the number of fitted parameters and guaranteeing that every fitted equation is thermodynamically consistent.","title":"Thermodynamic constraints"},{"category":"section","location":"deriving/thermodynamic_constraints/#Haldane-and-Wegscheider-relations","page":"Thermodynamic constraints","text":"Two kinds of constraint arise (Haldane, 1930) (Wegscheider, 1902), one for each kind of cycle.\n\nA Haldane constraint comes from a cycle whose net change matches the overall reaction — there is one per independent cycle that turns substrate into product. Its dependent rate constant is a power product that carries Keq, so the forward and reverse rates around the cycle cannot be chosen independently of the equilibrium constant. For the reversible Michaelis–Menten mechanism the reverse catalytic rate is fixed by Keq, the two binding constants, and the forward rate:\n\nk_EP_to_ES = (1 / Keq) * K_P_E * (1 / K_S_E) * k_ES_to_EP\n\nA Wegscheider constraint comes from a cycle that closes on itself with zero net metabolite change — one per independent internal loop. Its dependent rate constant is a power product of the others with no Keq factor. These appear when the same enzyme form can be reached by more than one binding order: detailed balance forces the binding constants around the loop to multiply consistently. For a random-order mechanism in which substrates A and B can bind in either order, the four binding constants are tied:\n\nK_A_E * K_B_EA = K_B_E * K_A_EB","title":"Haldane and Wegscheider relations"},{"category":"section","location":"deriving/thermodynamic_constraints/#How-the-constraints-are-found","page":"Thermodynamic constraints","text":"The package builds the enzyme-form incidence matrix — one column per step, with +1 at the step's to-form and -1 at its from-form — and computes its exact-integer null space. Each null-space basis vector is one independent cycle, so the constraints come out linearly independent by construction; there is no redundant set to thin down. The whole computation uses exact rational arithmetic, never floating point, so the same cycles are reproduced exactly every time.\n\nEach cycle is then classified by its net metabolite change: proportional to the overall reaction makes it a Haldane cycle, zero makes it a Wegscheider cycle. If a mechanism is thermodynamically infeasible — a cycle whose constraint reduces to 0 = c · log(Keq) with c ≠ 0 — the package raises an error rather than emitting a silent or wrong equation.","title":"How the constraints are found"},{"category":"section","location":"deriving/thermodynamic_constraints/#The-choice-of-independent-vs-dependent-parameters","page":"Thermodynamic constraints","text":"Each cycle costs one rate constant, and the package chooses which constant to keep and which to express in terms of the others by a fixed priority, designed to keep the biochemically meaningful parameters independent:\n\nFree-enzyme binding constants — the affinity of a substrate or product for the free enzyme, such as K_S_E — are kept independent whenever possible. These are the quantities an experimentalist measures and reports.\nBinding steps on already-occupied enzyme forms are eliminated next, when a cycle needs them.\nInternal isomerization rates are made dependent first: they are the most eliminable and the least directly measurable.\n\nWithin a steady-state step, the reverse rate is preferred dependent over the forward rate. Because step and group order are canonicalized when a mechanism is constructed, this choice is deterministic: two mechanisms with the same structure written in a different step order produce the identical reduced equation.","title":"The choice of independent vs dependent parameters"},{"category":"section","location":"deriving/thermodynamic_constraints/#A-concrete-example","page":"Thermodynamic constraints","text":"A random-order mechanism — two substrates that can bind in either order — carries both kinds of constraint at once: a Wegscheider tie among its binding constants and a Haldane tie on its catalytic step.\n\nusing EnzymeRates\nm = @enzyme_mechanism begin\n    substrates: A, B\n    products:   P\n    steps: begin\n        E + A ⇌ E(A)\n        E + B ⇌ E(B)\n        E(A) + B ⇌ E(A, B)\n        E(B) + A ⇌ E(A, B)\n        E(A, B) <--> E(P)\n        E(P) ⇌ E + P\n    end\nend\nprint(rate_equation_string(m))\n\nThe # Wegscheider constraints: section ties the binding constants of the two orders together (no Keq), and the # Haldane constraints: section fixes the reverse catalytic rate from Keq and the forward constants. Two rate constants are therefore dependent and do not appear in the fitted-parameter list:\n\nparameters(m)\n\nBoth eliminated constants are absent from parameters(m), so the fit explores only the thermodynamically independent directions — every candidate equation the fitter evaluates already satisfies detailed balance.\n\nSee also: parameters, rate_equation_string.","title":"A concrete example"},{"category":"section","location":"deriving/cha_king_altman/#The-Cha-/-King–Altman-algorithm","page":"The Cha / King–Altman algorithm","text":"EnzymeRates derives rate equations symbolically with the King–Altman schematic method (King and Altman, 1956), extended by Cha's rapid-equilibrium treatment (Cha, 1968). What makes this practical is Julia's metaprogramming: the whole derivation runs at compile time inside @generated functions. The first time rate_equation is evaluated for a given mechanism, the package performs the full symbolic derivation once and writes the resulting closed-form expression directly into the compiled method. Every later call is just that expression — a plain arithmetic formula with no symbolic machinery left behind, no heap allocations, and a runtime on the order of 100 ns on a modern processor. The fitter can therefore evaluate the rate equation millions of times per cross-validation fold and never pay the symbolic cost again.","title":"The Cha / King–Altman algorithm"},{"category":"section","location":"deriving/cha_king_altman/#Overview-of-the-algorithm","page":"The Cha / King–Altman algorithm","text":"The mechanism graph is partitioned into rapid-equilibrium (RE) segments — sets of enzyme forms linked by RE steps — connected to each other by SS steps.\n\nWithin each RE segment, the package picks the form with the fewest bound metabolites as the segment root (the free-enzyme reference form), with ties broken toward no covalent residual and then by name. Every other form in the segment gets an alpha factor — its relative abundance expressed as a ratio to the root. Referencing to the free-enzyme form is what produces the readable 1 + S / K_S_E + P / K_P_E denominator in the final equation.\n\nAcross segments, the SS steps form an inter-segment rate matrix. The Cha numerator and denominator come from cofactor determinants of that matrix, with each diagonal entry weighted by the sigma sum of alpha factors for its segment.","title":"Overview of the algorithm"},{"category":"section","location":"deriving/cha_king_altman/#Keeping-the-equation-finite-at-zero-concentration","page":"The Cha / King–Altman algorithm","text":"The derived rate equation is division-free in the concentrations: no metabolite concentration ever appears in a denominator, so the equation contains no 1/[S]-style terms anywhere.\n\nThis matters for fitting real data. Datasets often include measurements where a substrate or product concentration is exactly zero — a saturation curve that starts at zero substrate, or an initial-rate measurement taken before any product has formed. An equation with a 1/[S] term would diverge at those points and the fit would fail. Because the rate equation has no such terms, it stays finite at every concentration, including zero, and those data points are fit like any other.","title":"Keeping the equation finite at zero concentration"},{"category":"section","location":"identify/model_selection/#Best-mechanism-selection","page":"Best mechanism selection","text":"After the rate-equation search fits all candidate mechanisms, identify_rate_equation selects the simplest one that generalizes: the rate equation with the fewest parameters that best predicts kinetic data it was never fit on. This is the bias–variance tradeoff — adding parameters always improves the fit to the data in hand, but past a point the extra freedom tracks noise rather than signal and predicts new measurements worse, while too few parameters miss real structure. Cross-validation finds the balance by scoring each candidate on data held out from fitting.\n\nSelection happens in two phases. While the search runs, a beam keeps only the most promising mechanisms at each parameter count and expands those into the next count. After the search, leave-one-group-out cross-validation and a two-test rule pick the single best among the fitted candidates, guarding against an over-simple model whose better CV score is within noise.","title":"Best mechanism selection"},{"category":"section","location":"identify/model_selection/#Running-the-search","page":"Best mechanism selection","text":"The enumeration engine generates every candidate the beam fits. init_mechanisms builds the minimum-parameter starting mechanisms (or seed_mechanisms starts from fully-regulated ones when a reaction's regulators are required), and expand_mechanisms grows each survivor through single moves — splitting a shared rate constant, flipping a rapid-equilibrium step to steady state, adding a regulator, or making the enzyme allosteric. Every move yields slightly more complex children, so the beam meets candidates in order of increasing complexity.\n\nThe search walks parameter counts in ascending order. At each count it fits every candidate, keeps a beam of the most promising, and expands only those survivors into the next count. A mechanism at parameter count n stays in the beam if either condition holds:\n\nLoss cutoff — its loss is within\nmin(loss_rel_threshold * best(n) + loss_abs_threshold,\n    loss_parsimony_threshold * best(<n))\nwhere best(n) is the lowest loss at parameter count n and best(<n) is the lowest loss over all smaller counts — an added parameter must beat the best simpler model of any size. The best(<n) term is dropped at the base count (no smaller level exists yet).\nWidth floor — a cumulative per-count budget. The floor keeps expanding mechanisms at a parameter count until min_beam_width of them have been expanded over the whole search, then stops; after that only the loss cutoff admits at that count. The budget is spent once, not re-granted each time the count is revisited.\n\nThe four knobs, all tunable kwargs of identify_rate_equation:\n\nKeyword Default Meaning\nloss_rel_threshold 2.0 Relative tolerance: keep losses within this factor of best(n), the best at the same parameter count.\nloss_abs_threshold 0.01 Additive tolerance: guards against best(n) approaching zero (simulated / very-low-loss data), where a purely multiplicative cutoff would collapse the beam to the single best mechanism.\nloss_parsimony_threshold 1.01 An added parameter must earn its keep: keep expanding only if the loss is within this factor of best(<n), the best model of any smaller parameter count. Set to Inf to disable it.\nmin_beam_width 50 Cumulative floor: expand at least this many mechanisms per parameter count over the whole search. Spent once, then only the loss cutoff admits at that count.\n\nThe search ends when the frontier empties — when no surviving mechanism yields a new candidate to fit. Three limits drain it: max_param_count drops any child with more fitted parameters than the cap, eq_complexity_filter drops any child whose rate equation exceeds the complexity bound (both before fitting), and the enumeration itself runs dry — every move on every survivor reaches only mechanisms already fit or already dropped. The complexity bound roughly counts the terms in a rate equation's denominator; its default (337) passes a fully steady-state random-order bi-bi and skips anything denser. The beam then hands its pool of fitted candidates to the cross-validation rule below.","title":"Running the search"},{"category":"section","location":"identify/model_selection/#Leave-one-group-out-cross-validation","page":"Best mechanism selection","text":"Each unique value in the :group column is one fold. A group is one independent experiment — rates measured at a single enzyme amount across varying metabolite concentrations. For each fold the mechanism is fit on all other groups and scored on the held-out group, with the fold score floored at eps(Float64) so the log stays finite even when a single-row group fits exactly. Leaving one group out this way estimates how well the mechanism predicts a new experiment — a new enzyme batch, a different dilution, or a distinct assay plate.\n\nThe CV score for a mechanism is the mean of the log per-fold losses:\n\ncv_score = mean(log.(fold_scores))\n\nThis is not the mean of raw fold losses. Working in log space puts all fold scores on a comparable scale regardless of the absolute rate magnitudes and penalises extreme fold misses more gracefully than a linear mean would.","title":"Leave-one-group-out cross-validation"},{"category":"section","location":"identify/model_selection/#The-cross-validation-selection-rule","page":"Best mechanism selection","text":"Let n_min be the parameter count with the lowest mean log CV score (with a parsimony tiebreak to the smaller count). The selection rule then checks each simpler bucket (ascending n_params < n_min). A simpler bucket is accepted only if it passes both:\n\nPaired 1-SE rule (Hastie et al., 2009): the mean of the paired log-fold-loss differences (simpler − n_min) must not exceed se_threshold × std(diffs) / √n_folds. The default se_threshold=1.0 is the textbook one-standard-error rule.\nOne-sided sign-flip permutation test: the p-value Pr(perm_mean ≥ observed) under the sign-flip null must exceed perm_p_threshold. The default perm_p_threshold=0.16 matches the paired 1-SE criterion: under a normal approximation a ±1-SE band covers 68.3% of the distribution, leaving 1 − 0.683 = 0.317 in the two tails and ≈ 0.16 in the single tail the one-sided test uses. The test asks how often a random sign-flip of the per-fold loss differences would look at least as favorable to the simpler model; a high p-value means the simpler model is not meaningfully worse.\n\nThe function returns the smallest n_params that passes both tests, or n_min if none pass. When there is only one fold (one group), the SE is undefined and the loop is skipped, so n_min is always returned. Within the chosen bucket, the mechanism with the lowest training loss wins — CV scores rank buckets, training loss resolves ties inside a bucket.\n\nBoth se_threshold and perm_p_threshold are tunable kwargs of identify_rate_equation.","title":"The cross-validation selection rule"},{"category":"section","location":"identify/model_selection/#The-cv_results-DataFrame","page":"Best mechanism selection","text":"IdentifyRateEquationResults exposes exactly two fields:\n\nbest::AbstractEnzymeMechanism — the selected mechanism.\ncv_results::DataFrame — LOOCV results for every candidate that entered cross-validation.\n\ncv_results columns:\n\nColumn Description\nn_params Actual fitted-parameter count.\nloss Training loss (used for within-bucket ranking).\nmechanism_type Julia type name of the compiled mechanism.\nrate_equation Full symbolic rate-equation string.\nretcode Optimizer return code (\"Success\" = converged).\nerror Exception text if the fit errored; otherwise missing.\neq_hash Hex hash of the comment-stripped rate equation. Two mechanisms with the same eq_hash compute the same rate function.\none per fitted parameter Fitted parameter value, or missing if the mechanism lacks that parameter.\ncv_score Mean of log per-fold losses (lower is better).\nmean_log_loss_diff Mean paired log-fold-loss difference vs the n_min bucket's representative. 0.0 for n_min.\nse_paired Paired standard error: std(diffs) / √n_folds. 0.0 for n_min.\npermutation_p One-sided sign-flip p-value. 0.0 for n_min.\ncv_fold_<group> Per-fold test loss for held-out group <group>, one column per group.\n\nOnly the top n_cv_candidates distinct equations per parameter count enter LOOCV (default n_cv_candidates=5, a kwarg of identify_rate_equation). Distinctness is by eq_hash.\n\n","title":"The cv_results DataFrame"},{"category":"section","location":"#EnzymeRates.jl","page":"Home","text":"EnzymeRates.jl identifies the best enzyme rate equation from kinetic data. Given a reaction definition and rate measurements at varying substrate and product concentrations, the package enumerates the biochemically valid mechanisms, fits each to the data, and selects the simplest equation that describes the data.","title":"EnzymeRates.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/DenisTitovLab/EnzymeRates.jl\")","title":"Installation"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Getting Started — an end-to-end example from reaction to identified rate equation.\nDeriving rate equations — how the package turns a mechanism into a symbolic rate law.\nFitting rate equations — fitting a rate equation to kinetic data.\nIdentifying the best rate equation — the model-selection search.\nAPI Reference — every exported name.","title":"Where to go next"}]
}
