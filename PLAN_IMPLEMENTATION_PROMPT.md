You are one of series of agents whose goal is to implement this plan `~/.claude/plans/mossy-stirring-squirrel.md`. The spec for the whole package is in `SPEC.md`. Review the plan and progress made by previous agents. The plan is too large for one agent to implement so pick one unfinished step from the plan (ONLY ONE) and implement it, including adding the necessary tests. Confirm that all tests for the whole package pass by running `julia --project -e 'using Pkg; Pkg.test()'` (ie make sure you didn;t break any other functionality), write a report about your progress, append the plan noting which step was completed and leaving any useful comments about implementation details (do not rewrite the plan but simply append progress at the end), commit the new code with a helpful message.

IMPORTANT: If you review the plan and determine ALL steps are already complete (no unfinished steps remain), do NOT make any code changes. Instead, create a file called `.ralph-done` containing a brief summary of what was implemented, then exit.
